# V0.6 — 分层 QC + SceneGroup V2 变更说明

接着 A1/B1 之后，按原计划完成第 2、3 项。测试：**86 passed**（此前 64），6 个失败仍是沙箱环境所致（ffmpeg 4.4 无 `-fps_mode`、CanonCGT 测试调用 Windows conda python），与改动无关。

---

## 1. 改了哪些文件

| 文件 | 改动 |
|---|---|
| `color_core/project_qc.py` | **重写**。按 transform 角色分流；中性轴改测色度漂移；肤色改 ΔE2000；时间码化复核清单 |
| `color_core/scene_grouping.py` | **重写**。约束式凝聚聚类替代相邻贪心 |
| `color_core/color_metrics.py` | 新增 `rec709_to_lab` / `delta_e2000_rec709` |
| `color_core/face_analysis.py` | 新增 `mean_skin_rgb`（供 ΔE2000 使用） |
| `color_core/project_recipe.py` | `SceneGroup` 新增 `diagnostics` |
| `workers/job_worker.py` | 每个 LUT 带上真实角色；新增分组参数 |
| `color_core/canoncgt_provider.py` | CanonCGT 拟合 LUT 标记为 `creative` |
| `tests/test_layered_qc.py` | **新增** 11 项 |
| `tests/test_scene_grouping_v2.py` | **新增** 11 项 |
| `tests/test_v03_grade_plan.py` | 修正"分组必须连续"的旧假设 |

---

## 2. 分层 QC

### 根因：能力在，没接上

`inspect_cube_lut` 在 V0.5.2 就已经有 `transform_type` 参数，且对 `technical` 会跳过范围和中性检查。但唯一的调用方写的是：

```python
lut_reports = [inspect_cube_lut(path, "combined") for path in (lut_paths or [])]
```

每个 LUT 都按创意 LUT 判。这就是 48 个警告的来源。

现在 `_bake_safe_shot_lut` 会返回角色 —— 它是唯一真正知道这张 LUT 里有没有创意层的地方：

```python
if not shot.enabled:                  return "technical"
if group.creative_transform:          return "combined" if shot.look_strength * transform.strength > 0 else "technical"
return "combined" if shot.look_strength > 0 else "technical"
```

按你之前的报告，136 个镜头里有 **71 个 look_strength = 0** —— 这批现在被正确判为 `technical`。

### 更重要的：换了度量

原来测的是 `neutral_axis_max_spread`（中性轴的**绝对**偏离）。问题在于**技术白平衡本来就该移动中性轴**，测绝对值必然误报。

新度量是 `neutral_axis_chromaticity_drift`：中性灰阶上**色度的变化量**。它区分的是：

- 白平衡把整条轴**均匀**平移 → drift ≈ 0 → 正常
- 旧的对数域 slope 让轴**逐渐张开** → drift 很大 → 缺陷

两处实现细节是被测试逼出来的，不是设计时想到的：

1. **排除贴边采样**。通道增益 > 1 会在立方体顶端截断，截断样本的色度是截断的产物而非变换的产物。不排除的话，每一张带正增益的技术 LUT 都会误报。
2. **只测中段 0.15–0.95**。近黑处 ACEScct 是线性趾部而非幂律，色度数值不稳；近白处全都顶着钳位。

### 阈值是量出来的，不是拍的

我第一版阈值是凭直觉写的，实测下来全错了。重新烘真实 OCIO LUT 标定：

| LUT 类型 | drift（全域） | drift（0.15–0.95） |
|---|---:|---:|
| 合法技术校正（曝光/WB/对比/饱和 7 种组合） | 0.000 – 0.033 | **0.000 – 0.026** |
| 旧版对数 slope 缺陷 | 0.089 – 0.159 | **0.068 – 0.157** |

全域范围有重叠（0.033 vs 0.089 已经很近），中段限制后有干净的间隔。阈值取 **0.040**，落在 0.026 和 0.068 之间。

### 实测效果

```
=== 合法技术 LUT（role=technical）===
  exposure +0.8      PASS   drift=0.00000   warnings=[]
  exposure -0.8      PASS   drift=0.00000   warnings=[]
  wb mild            PASS   drift=0.00000   warnings=[]
  wb strong          PASS   drift=0.00000   warnings=[]
  wb+exp             PASS   drift=0.00000   warnings=[]
  wb+exp+con+sat     PASS   drift=0.00000   warnings=[]
  full technical     PASS   drift=0.02578   warnings=[]
  -> 合法技术 LUT 的总警告数：0

=== 旧版缺陷 LUT 必须仍被抓到 ===
  [0.93, 1.0, 1.12]  NEEDS_REVIEW  drift=0.07830
  [1.08, 1.0, 0.90]  NEEDS_REVIEW  drift=0.06756
  [0.88, 1.0, 1.12]  NEEDS_REVIEW  drift=0.15659
  -> 抓到 3/3
```

### 曲率检查被我删掉了

我原本加了个"曲率 > 阈值 = banding 风险"。实测数据：

| | 曲率 |
|---|---:|
| 合法技术 LUT | 0.081 – 0.276 |
| 创意 Look | 0.028 – 0.174 |
| 旧版缺陷 LUT | 0.149 – 0.197 |

**合法 LUT 的曲率比缺陷 LUT 还高。** 这个指标区分不出任何东西 —— 33³ 网格上的最大二阶差分被色调曲线的趾部/肩部主导，和 banding 无关。

我没有真实的 banding 样本可以标定它，所以：`max_curvature` 仍然**作为数值记录**在报告里，但**不再据此发警告**。与其发一个我无法证明其有效性的警告，不如诚实地只记录。真要做 banding 检测，应该测的是渲染后画面的实际条带，不是 LUT 网格。

### 肤色：先修了一个 bug

肤色色相旋转原本用最近网格点采样。实测**恒等变换也报 6.92°** —— 33³ 的量化误差被当成了色相旋转，等于给所有测量加了个固定偏移。改成三线性插值后恒等变换 = **0.00°**。

标定后的 Look 实测值：

| Look | 肤色旋转 |
|---|---:|
| neutral_broadcast | 0.04° |
| cinematic_soft_print | 0.97° |
| warm_memory | 2.80° |
| restrained_teal_amber | 4.39° |
| **cold_thriller** | **18.23°** |
| **day_for_night** | **119.71°** |

阈值创意层 12°、合成层 18°。`cold_thriller` 和 `day_for_night` 被标记为 NEEDS_REVIEW —— 它们确实在大幅改变肤色，这个提示是对的。

肤色偏移判定同时从 HSV 色相标量升级为 **CIEDE2000**（`face_analysis` 新增 `mean_skin_rgb`）。ΔE76 在肤色所在的橙色区误差很大，会漏报。

### 状态分层

```
render_integrity  ┐
base_correction   │
creative_transform├→ final_decision（取最差）
scene_continuity  │
skin_safety       ┘
```

新增 `within_group_continuity`：**组内跳变判 NEEDS_REVIEW，组边界跳变只判 PASS_WITH_WARNINGS** —— 同组镜头本该共享一个光位，组内跳变是真缺陷；组边界的跳变通常是有意的剪辑点。这直接对应报告里"15 个 WB 跳变中 14 个在组内"。

淡入淡出/黑场镜头（`content_flags.preserve_intent`）现在被排除在连续性判定之外。

`review_items` 现在带 `timecode`（HH:MM:SS.mmm）、`scene_group_id` 和 `suggested_action`（`match_hero` / `review_boundary` / `lower_look_strength` / `review_shot`），按时间排序。另有 `lut_warning_summary` 按角色汇总警告分布。

---

## 3. SceneGroup V2

### V1 的三个结构性问题

```python
for shot in shots[1:]:
    if len(current) < max_group_shots and distance(current[-1], shot) <= t:
        current.append(shot)
    else:
        start_new_partition()
```

1. **只能和相邻镜头合并** —— 对话戏的正反打在每次切机位时都断组
2. **`max_group_shots` 在合并条件里** —— 这是"多个组恰好 12 个镜头"的直接成因
3. **距离建立在编造的量上** —— `estimated_temp_offset = gain_b - gain_r`

### V2

**距离**用 B1 带来的物理量：mired（色温的感知均匀单位）、Duv、log2 亮度、彩度、构图 embedding、人脸占比。各维用 MAD 稳健归一化。缺失维度（比如没有 embedding）会**按可用维度重新归一化权重** —— 否则一个固定的 0.5 惩罚会吃掉一半阈值预算，导致什么都合并不了（这个是写测试时发现的）。

**聚类**改为约束式凝聚聚类（UPGMA 平均连接）：

- 反复合并**距离最小**的两个簇，不限于相邻 → 正反打能重新合到一起
- 约束：时间跨度间隔 ≤ `time_window`（默认 45 秒）、合并后 ≤ `max_group_shots`
- 用 Lance-Williams 闭式更新平均连接距离，候选搜索向量化 → 136 镜头是毫秒级
- `argmin` 在 C-order 数组上向低索引解并列 → **完全确定性**

**`max_group_shots` 默认 12 → 24**，且只作为性能保护，不再驱动分组。

**Hero Shot** 改为以"到组内统计中心的距离"为主导权重（−2.0），时长只占 0.5。Hero 是全组的匹配基准，必须是分布的中心，而不是最长的那个 —— V1 主要按时长和位置选，很容易选中一个坐在颜色分布边缘的长镜头。

**后验校验**：分组完成后测组内 mired 跨度和亮度跨度，超限就沿更受违反的那个轴在中位数处拆分。分组器自己检查自己的输出。

**`SceneGroup.diagnostics`** 记录 `mired_spread` / `median_cct_kelvin` / `luminance_spread_stops` / `coherent`，分组的一致性声明变成可审计的。

### 实测对比

6 个镜头，暖/冷两种光位交替（A B A B A B），同一场景内：

```
V1 分组: [[shot_0001], [shot_0002], [shot_0003], [shot_0004], [shot_0005], [shot_0006]]
V2 分组: [[shot_0001, shot_0003, shot_0005], [shot_0002, shot_0004, shot_0006]]
V2 诊断: group_0001 中位 4392K, mired 跨度 0.0
         group_0002 中位 11579K, mired 跨度 0.0
```

**V1 把 6 个镜头分成了 6 个组** —— 每次切机位都断。V2 正确识别出两个光位，各自 mired 跨度为 0。

---

## 4. 破坏性变更

### 4.1 分组不再连续 ⚠️

一个 SceneGroup 现在可能在时间上和另一个交错（正反打）。**任何假设"组是连续区间"的代码或 UI 都需要检查。** `tests/test_v03_grade_plan.py` 里就有这样一条断言，已改为断言覆盖性（每个镜头恰好出现一次）而不是顺序。

前端 `app.js` 的时间线 SceneGroup 蒙太奇预览需要你确认一下：它是按组拼接镜头的，交错组会产生跳跃的蒙太奇 —— 这在语义上是对的（同一光位的镜头放一起看），但视觉上和以前不同。

### 4.2 分组数量和边界会变

同一条素材重跑，SceneGroup 的数量、成员和 Hero Shot 都会变。**已有的 GradePlan 里的 `approved_candidate_id` 是按 group 记录的**，重新分组后这些绑定会失效。建议：分组变更后重新走候选选择和批准。

### 4.3 QC 报告字段变化

新增：`base_correction`、`creative_transform`、`within_group_continuity`、`lut_warning_summary`，`LUTQuality` 新增 `neutral_axis_chromaticity_drift` / `max_curvature` / `skin_hue_rotation_deg` / `state`。

保留：`technical_color`（= base 和 creative 的较差者），旧字段都还在。

---

## 5. 测试时预期看到什么

### 应该改善

1. **LUT 警告大幅下降。** 71 个 `look_strength=0` 的镜头现在按 technical 判，实测合法技术 LUT 的警告数为 0。原来 48 个警告里，"超范围采样"（46 个）和"中性轴分离"（34 个）这两类应该基本消失。
2. **组内白平衡跳变减少。** 正反打不再被拆散，同光位镜头进同一组共享 Hero。原来 14 个组内 WB 跳变应该明显下降。
3. **不再有"恰好 12 个镜头"的组。**
4. **QC 报告可操作**：每条带时间码、组 ID 和建议动作。

### 需要你判断的

- `cold_thriller`(18°) 和 `day_for_night`(120°) 的肤色旋转警告 —— 这是设计意图还是需要收敛，你说了算
- `time_window=45` 秒是否合适。长镜头多的片子可能需要调大（`grouping_time_window` 参数）
- 分组数量的变化是否符合你对这条片子的理解

### 仍然没做

- 曝光仍是全画面中位数驱动（C1）
- 统计仍在 gamma 域（A3）
- 低置信度镜头仍直接放弃白平衡（C2）
- banding 没有真正的检测手段（见上）
- 渲染链仍是 8-bit（D1/D2）

---

## 6. 验证步骤

```powershell
& C:\Users\Administrator\.conda\envs\localcolor\python.exe -m pytest tests/test_layered_qc.py tests/test_scene_grouping_v2.py -v
& C:\Users\Administrator\.conda\envs\localcolor\python.exe -m pytest -q
```

然后拿那条 4分18秒 / 136 镜头的素材重跑，对比 `project_quality_report.json`：

```
lut_warning_summary.technical.with_warnings   ← 期望接近 0
lut_warning_summary.combined.with_warnings    ← 创意层的警告，应保留
within_group_continuity                       ← 期望从 NEEDS_REVIEW 改善
scene_group_count                             ← 会变，检查是否符合你对这条片子的理解
```

以及每个 `scene_groups[].diagnostics.mired_spread` —— 这是分组质量最直接的数字。

---

## 7. 一点说明

这轮有三处是**测试和实测数据推翻了我的初始设计**，值得记下来：

1. 中性轴漂移的第一版阈值（0.010）会误报所有真实技术 LUT —— 必须排除贴边采样并限制到中段，阈值实测标定为 0.040
2. 曲率作为 banding 指标完全无效（合法 LUT 的曲率比缺陷 LUT 还高）—— 已移除该警告
3. 肤色色相旋转用最近网格点采样，恒等变换就有 6.92° 的假读数 —— 改三线性插值后归零

前两条如果没有实测就直接上线，会用一批新的误报替换掉旧的误报 —— 那等于没修。
