# V0.6.6 — 两轮流程跑完后的日志排查与修复

测试：**184 passed**（此前 177），6 个失败仍是沙箱环境。

---

## 先看两轮流程的结果

| 模式 | 任务链 | 耗时 |
|---|---|---|
| 模式 1（AdaInt） | scenes 1.4min → project_recipe → scene_group_adaint 0.7min → project_render 6.5min → approve → project_render 6.5min | — |
| 模式 2（CanonCGT） | reference_preflight ×2（0.02/0.13min）→ reference_candidates 11.4min → project_render 6.7min | — |

全部 completed，没有失败。preflight 现在 1–8 秒完成，双车道队列有效。

### P0-1 / P0-2 都生效了

| 指标 | V0.6.3 | 本轮 |
|---|---|---|
| **高光裁切** | 10 个（最高 21.7%） | **0 个** ✅ |
| 肤色风险 | 40 个（ΔE2000 中位 8.61） | 17–29 个，全部归类 `skin_hue_shift` ✅ |
| LUT 警告 | 0 | 0 |

高光保护完全奏效。

---

## 问题一：人脸保护静默失效了 🔴

日志里这行出现了 **4 次**，覆盖本轮全部渲染：

```
[WARNING] Face-selective render unavailable; using normal timeline render:
          expected str, bytes or os.PathLike object, not list
```

**这是我在 V0.6.2 引入的回归。** 那次把技术层和创意层改成链式 LUT（`face_specs` 里传 list），但 `render_face_selective_timeline` 里这段没跟上：

```python
protected_spans = [
    (start, end) for start, end, full_lut, face_lut in shots
    if Path(full_lut).resolve() != Path(face_lut).resolve()   # full_lut 是 list → TypeError
]
```

异常被外层 `except` 捕获，退回普通时间线渲染 —— **从 V0.6.2 起人脸保护一次都没真正跑过**，而且只留下一行 WARNING。

已修（`_stack()` 统一处理单路径与链式），并用链式 list 跑通了完整的遮罩生成 + ffmpeg 渲染验证。

> 教训：这个 `except Exception` 把一个类型错误变成了"功能静默降级"。降级本身是对的（渲染不该失败），但降级原因是 TypeError 时应该更醒目。

---

## 问题二：连续性 QC 的口径在误导 🟠

报告里"白平衡跳变 29 个"看起来很糟。但把源片同一批接缝量一遍：

```
源片 135 个接缝里，本来就超过 25 mired 的有 65 个（中位 23.9，最大 146.2）
成片被标记的                                    29 个
```

**调色把超阈值的接缝从 65 个降到了 29 个 —— 连续性其实改善了一半以上**，却被报成"29 个问题"。

再拆开这 29 个：

| | 数量 |
|---|---|
| 调色后**更好**（比源片更接近） | **16** |
| 基本不变 | 2 |
| 调色后**更差** | **11** ← 只有这些是真问题 |

绝对阈值判的是"成片的接缝有多大"，但那很大程度上是**素材本身**的属性。该判的是**调色让接缝变好还是变坏**。

已改为相对源片的口径（`delta > source_delta × 1.15` 才算缺陷，并记录 `source_delta` 和 `worsened_by`）：

| | 旧口径 | 新口径 |
|---|---|---|
| 白平衡跳变 | 29 | **11** |
| 亮度跳变 | 25 | **15** |

保留下来的每一条都是调色真的推开的接缝，带源片对照值。

（没有渲染产物时退回绝对阈值——那时没有"之后"可比。）

---

## 问题三：调色在组边界引入新跳变 🔴

新口径留下的 11 条，最严重的：

```
shot_0117→shot_0118   源片Δ= 4.5 mired  →  成片Δ=41.6 mired  (+37.1)
shot_0057→shot_0058   源片Δ= 2.5 mired  →  成片Δ=35.7 mired  (+33.1)
shot_0126→shot_0127   源片Δ= 4.8 mired  →  成片Δ=36.5 mired  (+31.7)
```

源片里几乎一样的相邻镜头，调色后被推开 30–40 mired。查下来是三个叠加的原因：

### 3a. 整组读不出光源时被完全跳过

```
shot_0126  group_0022   共识=None  ← 整组没有自信成员
shot_0127  group_0006   共识=113.7
shot_0128  group_0022   共识=None
```

C2 原来的逻辑是"整组都没把握就不动它"。听起来保守，实际后果是：**邻居都被校正了，这个组成了未校正的孤岛**，每个进出它的剪辑点都变成跳变。25 个组里有 5 个是这状态，而且贡献了最大的几个引入量。

**改法**：整组读不出光源时，向**时间上相邻的组**借共识（取邻组共识的中位数）。这是"低置信镜头继承组内共识"的同一思路，往上提一层。真的连邻组也读不出，才保持不动并在诊断里说明。

### 3b. 组边界没有连续性约束

每个 SceneGroup 各自朝自己的共识白点校正，完全不知道剪辑上下一个镜头是什么。

**改法**：在组共识之后加一轮**边界松弛**——一个剪辑点最多只能和源片一样不同，外加 12 mired 容差。松弛在**组级别**进行（整组一起移动），所以组不会为了迁就边界而牺牲内部一致性。

### 3c. 增益夹取本身是不连续性的来源

原来把应用增益夹到 `[0.80, 1.25]`。实测这个夹取是**硬饱和器**：

| 目标 mired | 夹取后实际 mired |
|---|---|
| 210 | **198.0** |
| 230 | **198.0** |
| 260 | **198.0** |

三个差别很大的目标塌缩到同一个值；而且范围不对称（相对 D65 是 −29/+43 mired）。**刚好在包络内的镜头和刚好在包络外的镜头会被拉开一大截** —— 夹取自己在制造跳变。

**改法**：改成在 **mired 空间限制白点位移量**（`limit_white_point_shift`，默认 ±40 mired）。平滑、单调、对称，而且意图直白："不要把白点移动超过这么多"。

### 结果（同一条 136 镜头素材模拟）

| | 推开的接缝 | 最大引入 | 总引入 |
|---|---:|---:|---:|
| 修复前 | 8 | **36.8 mired** | 125.5 |
| 修复后 | 8 | **14.3 mired** | 83.6 |

最大引入量降到约 1/3，剩下的都落在 12 mired 容差线附近，属设计内。

---

## 顺带修的一个组内一致性 bug

写回增益时有个守卫：

```python
if abs(item["target_mired"] - item["source_mired"]) < 0.5:
    continue     # 跳过"没变化"的写入
```

它比较的是**目标 vs 源片**，于是一个源片读数恰好接近组共识的镜头会**保留自己的独立增益**而不是组共识 —— 组内一致性就此破掉。这正是组共识存在的唯一理由。

改成无条件写入组的增益，只在数值真的变化时才计入 `adjusted`。

---

## 破坏性变更

1. **`APPLIED_GAIN_MIN/MAX` 移除**，改用 `white_balance.limit_white_point_shift`（±40 mired）。强色偏镜头的校正量会与之前不同。
2. **连续性 QC 字段新增** `source_delta` 和 `worsened_by`；跳变数量会显著下降（这是口径修正，不是画面变化）。
3. **人脸保护会真正开始工作**——之前四轮渲染其实都没跑它。渲染时间会比这几次长（多一路 lut3d + maskedmerge）。
4. C2 诊断新增 `inherited_from_neighbours`。

---

## 重跑时看什么

```powershell
& ...\python.exe -m pytest -q
```

```
日志里不应再出现 "Face-selective render unavailable"
project_quality_report.json:
  adjacent_temperature_jumps   期望 ≈ 11 而不是 29，每条带 source_delta / worsened_by
  adjacent_luminance_jumps     期望 ≈ 15 而不是 25
  clipping_scenes              保持 0
white_balance_diagnostics:
  inherited_from_neighbours    看有几个组是借来的白点
  仍为 None 的组               这些是连邻组都读不出的，值得人看
data/jobs/<id>/face_protection_mask.avi   应该存在且是 480x270 gray
```

---

## 这轮的一点观察

三个问题里有两个是**报告口径**而非画面质量：连续性用了绝对阈值、人脸失效只留一行 WARNING。真正的画面缺陷（组边界跳变）反而是被修正后的口径才暴露出来的。

配合上一轮"三次是测量方法出错"的记录，模式已经很清楚：**这个系统现在最容易出错的地方不是算法，是我们怎么衡量算法。** METRICS.md 的存在价值也在这里。
