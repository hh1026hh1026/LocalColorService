# V0.6.8 — 以评价人 B 为基准的校准

测试：**213 passed**（此前 205）。

---

## 做法

把 `review_a0dbcffde053(1).csv` 的 44 条判断当作真值，逐个特征算它对 fix/ok 的分离度（AUC），只调整**真正分得开**的那些。

```
特征            fix中位    ok中位     AUC   区分力
req_ev         0.5060   0.3170   0.715   强   ← 唯一
d_sat         -0.0282  -0.0009   0.316   中
sat_out        0.2531   0.3098   0.338   中
clip_out       0.0011   0.0003   0.645   中
skin_rot       5.1200   6.9500   0.366   中（反向）
warm           2.0798  15.9295   0.390   弱（反向）
abs_warm      19.9443  19.9693   0.523   无
```

---

## 唯一被验证的调整：大幅曝光标记

新增 QC 类别 `large_exposure_correction`，阈值 **0.50 EV**。

- 45 镜头里唯一能区分"需要修 / 可接受"的量，AUC 0.715
- **第二位评价人独立得到同方向**（0.51 vs 0.25），虽然他整体一致性差，但这一条对上了
- 取 0.50 而非 Youden 最优的 0.35：精确率 0.82 vs 0.75。四分之三为真的清单会被跳读，五分之四为真的会被读完

各标记在同一批数据上的实际表现：

| 标记 | 标出 | 精确率 | 召回 |
|---|---:|---:|---:|
| `skin_hue_shift` @ 6° | 25 | **0.56** | 0.52 |
| `highlight_limited` | 28 | 0.71 | 0.74 |
| **`large_exposure` @ 0.50 EV** | 17 | **0.82** | 0.52 |
| 曝光 + highlight_limited | 32 | 0.72 | **0.85** |

复核清单改用最后一组。全片 136 镜头中 23 个会被曝光标记选中。

---

## 三个被数据否定的改动（都没做）

### 1. 肤色色相阈值：方向是反的

```
判 fix 的镜头  肤色旋转中位 5.1°
判 ok  的镜头              6.9°     ← 更大
AUC 0.366
```

评价人**接受**的镜头肤色旋转反而更大。第一位评价人也是（5.8° vs 5.3°，几乎无差别）。

6° 这个阈值标出的 25 个镜头精确率只有 0.56 —— 接近抛硬币，不值得占用复核时间。

**处理**：阈值从 6° 放宽到 **20°**（只保留 `cold_thriller` 33°、`day_for_night` 114° 这类无争议的），肤色旋转仍然照常测量和记录。代码里写明了"NOT VALIDATED AGAINST HUMAN JUDGEMENT"以及需要什么样的专项测试才能定这个数。

同时新增 `skin_clipped` 到复核表 —— 肤色被推到范围顶端是**无歧义的损伤**，和"色相偏了"是两回事。

### 2. 暖化上限：不成立

我上一轮从备注里推出"+20 mired 以上的暖化会被抱怨"。用评分验证：

```
判 fix 的镜头  暖化中位  +2.1 mired
判 ok  的镜头            +15.9 mired    ← 更暖
绝对值 AUC 0.523（完全无区分力）
```

被接受的镜头暖化反而更多。`MAX_CORRECTION_MIRED` **保持 40 不变**。

（备注里 14 条"偏黄"确实对应 +17.7 mired 的暖化，但 ok 的镜头也有 +15.9 —— 暖化是必要不充分条件，单独不能作为判据。）

### 3. 对比度导致去饱和：实测推翻

我根据 `d_sat` 的相关性猜"对比度提升在去饱和，需要补偿"。直接烘 LUT 量：

```
源图饱和度           0.4573
对比度 1.10 →       0.5042  (+0.0469)   ← 上升，不是下降
对比度 1.20 →       0.5469  (+0.0896)
```

机制不成立，没做这个修改。`d_sat` 的相关性另有原因，这批数据分不出来。

---

## 关于这个基准的局限（写进了 METRICS.md）

1. **只有一位评价人**。第二位 kappa 0.236，判断不可靠，只用来做交叉验证。
2. **只能衡量误报，不能衡量漏报**。复核表里只有系统已标出的镜头，系统漏掉的没人看过。表里的"召回"是"在已标出范围内的召回"。
3. **n=45**，单项目、单素材。
4. 备注和评分高度耦合（26 条备注对应 25 个 fix），不是两份独立证据。

所以 `LARGE_EXPOSURE_CORRECTION_EV = 0.50` 是**待复核的经验值**。下一批盲测数据到位后应重新拟合。

---

## 改了哪些

| 文件 | 改动 |
|---|---|
| `color_core/project_qc.py` | 新增 `large_exposure_corrections`；肤色阈值 6°→20°、ΔH* 3.0→8.0，附完整依据 |
| `scripts/build_review_sheet.py` | 复核表改用「大幅曝光 + 高光受限 + 肤色死白」，肤色色相降为次要 |
| `docs/METRICS.md` | 新增第 7b 节「人工基准」，含各标记精确率对照与三条被否定的假设 |
| `tests/test_human_calibrated_flags.py` | 新增 9 项 |

---

## 下一步

要让这个基准真正可用，缺的是**盲测**：

- 随机左右 + 不标注哪边是原片，问"哪一版更好"
- 结构化标签替代自由文本
- 插入重复镜头量同一个人的自洽度
- **抽一批系统没标出的镜头混进去** —— 这是当前最大的盲区，没有它就永远不知道漏了多少

前三条我可以直接改复核表生成器。第四条需要你确认抽样比例（建议未标记镜头占 30% 左右，且不告诉评价人哪些是哪些）。
