# Local Color Service V0.5.1 验收报告

验收日期：2026-08-04

## 交付内容

- 360p 分析代理缓存：实测使用 `nvdec+scale_cuda+nvenc`。
- 代表帧批量抽取与持久缓存，避免按镜头反复启动 FFmpeg。
- 整片和选段预览优先使用 H.264 NVENC，保留 CPU 编码回退。
- Shot 缩略图时间线、点击定位、当前镜头检查器。
- Shot 上下文预览（前后各 1 秒）与 SceneGroup 预览。
- 预览按 GradePlan revision、作用域、目标和规格缓存。
- 人工调整可作用于当前 Shot 或整个 SceneGroup。
- 自动操作包括恢复分析配方、匹配 Hero Shot 和本组 AdaInt。
- 参考图预检：技术 QC、适配度、预览图和使用规范。
- 性能状态接口明确报告 NVDEC、NVENC、CUDA Scale 和 LUT 后端。

## 实际验收

- 服务版本：`0.5.1`。
- RTX 3090 / CUDA 分析代理生成通过。
- 缩略图文件与受限 Job Artifact 路由通过。
- 时间线上下文预览输出：`data/jobs/4864128ba17c/timeline_preview.mp4`。
- 179 镜头项目选中 `scene_0002` 后生成前后文预览：`data/jobs/5f8e2da37c1b/timeline_preview.mp4`，包含 3 个相邻片段、时长 3.966667 秒、540p、音频保留并与画面同步。
- 参考图 `1.png` 预检通过，界面显示 1130×584、技术检查通过、适配度 68%。
- 浏览器完成镜头分析、项目配方、时间线镜头选择、选段预览和参考图预检；控制台无错误。
- Python/JavaScript 验证：`43 passed, 1 warning`，`node --check` 通过。

## GPU 边界

当前 FFmpeg 支持 NVDEC、`scale_cuda`、`colorspace_cuda` 和 NVENC，但没有 `libplacebo` 或其他 GPU 3D LUT 滤镜。V0.5.1 因此采用 GPU 解码/缩放/编码与 CPU 四面体 LUT 插值的混合管线，并在 UI 和 `/v1/performance/status` 中如实显示。
