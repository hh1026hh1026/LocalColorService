# V0.6.2 — C2 / A3 / C1 / C3 / D1-D2 变更说明

按顺序做完了 P1 队列剩余全部五项。测试：**144 passed**（此前 106），6 个失败仍是沙箱环境（ffmpeg 4.4 无 `-fps_mode`、CanonCGT 测试调用 Windows conda python）。

---

## 1. 改动总览

| 文件 | 项 | 改动 |
|---|---|---|
| `color_core/group_white_balance.py` | C2 | **新增**。组内共识白点 |
| `color_core/image_analyzer.py` | A3/C3 | 白平衡在线性光下估计；新增真实 CCT/Duv；支持空间权重 |
| `color_core/correction_engine.py` | C1/A3 | **重写**。肤色锚点曝光；EV 用线性比值 |
| `color_core/scene_analysis.py` | C3/C1 | 自适应帧数；空间权重图；调序让人脸先于配方 |
| `color_core/output_profiles.py` | D1/D2 | **新增**。浮点 LUT 管线 + 三档输出 |
| `color_core/cube_tools.py` | D2 | 新增 `scale_cube_strength` |
| `color_core/timeline_renderer.py` | D1/D2 | 链式 lut3d；浮点；master 档 |
| `color_core/renderer.py` | D1 | 浮点 LUT 管线 |
| `color_core/selective_renderer.py` | D1/D2 | 两个分支都走浮点链式 |
| `color_core/white_balance.py` | C2/A3 | 白点↔增益互转；线性化指数默认 1.0 |
| `color_core/project_qc.py` | A3 | 连续性判据改 mired |
| `color_core/grade_decision.py`、`plugin_interface.py` | A3 | 阈值改 mired / 增益包络放宽 |
| `workers/job_worker.py` | 全部 | 接入 |
| 新增测试 | | `test_linear_wb_and_skin_exposure.py`(23)、`test_render_precision.py`(15) |

---

## 2. C2 — 组内共识白点

### 问题

`image_analyzer` 在三个候选不一致时直接放弃：

```python
else:
    gain_r, gain_g, gain_b = 1.0, 1.0, 1.0
    selected_method = "preserve_low_confidence"
```

单镜头看是安全的，对序列是错的。**最难估计的镜头恰恰是最需要跟组的镜头**（混光、大特写、拍墙），结果它们不校正，而同一光位的邻居校正了。你报告里的"15 个 WB 跳变，14 个在组内"就是这么来的。

调色师不会逐镜头重判白平衡，而是先定这个光位的白点，再把组里所有镜头匹配过去。

### 实现

在 **mired / Duv** 空间（色温的感知均匀轴）按置信度加权求共识白点：

- 完全放弃的镜头 → **直接采用共识**
- 有把握的镜头 → 保留自己 70%，向共识收缩 30%
- 有把握但**偏离很大**（> 60 mired ≈ 1/3 CTO）→ **标记但不动**，那通常是画面里真有一盏实用光或一扇窗，强行拉平比跳变更糟
- 整组都没有可用估计 → 不编造共识，原样保留
- `base_grade_policy == "preserve"` 的镜头（黑场、淡变）完全跳过

共识用**加权中位数**而非均值，对组内单个离谱镜头稳健。

诊断写进 `project_recipe` 任务结果的 `white_balance_diagnostics`：

```json
{"scene_group_id": "group_0001", "adjusted": 7, "adopted_consensus": 3,
 "consensus": {"cct_kelvin": 4392.0, "mired": 227.7, "contributing_shots": 9},
 "flagged_different_light": [{"shot_id": "shot_0042", "cct_kelvin": 2810.0, "delta_mired": 128.3}]}
```

关掉：`project-recipe` 请求传 `harmonize_white_balance: false`。

---

## 3. A3 — 统计改线性域

灰世界的前提是"场景平均反射率中性"，这是**线性光**命题。在 gamma 编码域求通道均值比，暗部被 OETF 放大了权重，系统性低估偏色。

现在三个候选（灰世界、Shades-of-Gray、中性高光）全部在线性光下估计。

**连带的两个必须一起改的地方：**

1. **`WB_GAIN_LINEAR_EXPONENT` 默认 2.2 → 1.0。** V0.6 里那个 2.2 次幂是用来把显示域增益换算成物理增益的补偿；现在增益本身就是线性的，不需要了。留着会双重放大。
2. **候选一致性阈值 0.20 → 0.44。** 同样的分歧在线性域大约是显示域的 2.2 倍，不改的话大部分镜头会被判成低置信度。

`LuminanceMetrics` 现在同时给出显示域和线性域：连续性/跳变判据是感知比较，继续用显示域（那本来就是对的）；曝光 EV 和白平衡用线性域。

### 顺手替掉了一个编造的量

`estimated_temp_offset = gain_b - gain_r` 没有单位、没有物理含义，却被三个下游模块当色温用。现在报告里有真实的：

```
source_cct_kelvin, source_duv, mired_offset_from_d65
```

`grade_decision`、`project_qc` 连续性、`correction_engine` 的偏色判据全部改用 mired（QC 新增 `max_adjacent_mired_jump`，默认 25 mired ≈ 1/8 CTO）。旧字段保留，旧报告走回退路径。

---

## 4. C1 — 肤色锚点曝光

### 旧逻辑的两个问题

```python
if lum_med < 0.25:
    needed_ev = (0.40 - lum_med) * 2.0
```

1. **锚点错了。** 全画面中位数被天空、暗背景、letterbox 主导，而这些恰恰不该决定人的亮度。
2. **数值也错了。** 显示域中位数从 0.20 拉到 0.40 是**两档**，不是这个式子算出的 0.4 EV。旧值的保守不是设计选择，是算术意外。

`face_analysis` 一直在算 `mean_skin_luminance`，`correction_engine` 从来没用过。

### 现在

有人脸且占比 ≥ 1.5% 时，以肤色亮度为锚，EV 用**线性域 log2 比值**算。

**关键设计：用容差带而不是固定目标。** 把所有人脸都钉到同一个亮度会把深肤色拉亮、把浅肤色压暗，既不符合摄影也对被摄者不公平。所以肤色落在 `[0.45, 0.72]` 显示域区间内就完全不动，只有超出才拉到最近的边界。

**超出安全包络时会明说**，不再静默吞掉：

```json
{"requested_exposure_ev": 2.31, "exposure": 0.8, "exposure_anchor": "skin_anchor"}
```
> "Measurement asked for +2.31 EV; only +0.80 EV was applied. A shot needing more than the automatic envelope should be reviewed."

⚠️ **这会让 clamp 命中率明显上升**——因为真实需求本来就比旧公式算出来的大。这是正确行为（系统在说"我需要 2.3 档但只敢做 0.8 档"），但你会在报告里看到更多这类提示。

---

## 5. C3 — 代表帧与空间加权

- **帧数自适应**：按时长 3~9 帧（`duration/1.5`），运动镜头首尾差异大，3 帧不够
- **空间权重**：中心高斯加权（σ≈0.7 半幅，中心约为角落的 3 倍）+ **letterbox/pillarbox 区域权重归零**

`classify_special_content` 早就检测出黑边了，但**只用于跳过调色决策，从来没用来把黑边排除出统计**——这是第二个"现成信号被浪费"的地方，现在接上了。

实测（200×200，上下各 40px 黑边）：黑位裁切从 **40% → 0%**，均值从 0.376 → 0.627。

分析改成两遍：第一遍检测黑边，第二遍带权重重新测量。

---

## 6. D1/D2 — 渲染精度与母版

### 浮点 LUT 管线

以前 `lut3d` 直接作用在 8-bit `yuv420p` 上再写回 8-bit，两次量化。现在：

```
format=gbrpf32le, lut3d[, lut3d], format=yuv420p    (+ -sws_dither ed)
```

已实测确认 `lut3d` 接受 `gbrpf32le` / `gbrp16le` / `yuv444p16le` / `yuv444p10le`。

### 技术层与创意层改为串联

`compose_cube_luts` 把第一张 LUT 的输出在 33³ 网格上重采样通过第二张，用的还是三线性插值（渲染时是四面体）。实测对比浮点参考：

| | 平均误差 | p99 | 最大 |
|---|---:|---:|---:|
| 预合成（旧） | 3.9e-4 | 8.2e-3 | **0.088** |
| 串联（新） | 4.2e-5 | 1.2e-3 | **0.0079** |

**约 9 倍。** 最大误差 0.088 在 8-bit 上是 22 个码值，肉眼可见。

**一处需要更正**：我最初在代码注释里写串联的好处是"保住技术层推到 1.0 以上的高光"。实测下来烘出的技术 LUT 确实带 >1.0 的值（范围 −0.139 ~ 1.495，`CDL_NO_CLAMP` 生效），**但 ffmpeg 的 lut3d 无论如何都会把输入钳到 LUT 定义域**，所以串联并不能保住那部分高光。真正的收益来自避免网格重采样和 8-bit 量化。注释已改正。

部分强度通过 `scale_cube_strength` 把创意 LUT 向恒等混合后再串联，不再靠 compose 的 `second_strength`。

### 三档输出

| profile | 编码 | 像素格式 | 用途 |
|---|---|---|---|
| `preview` | H.264 CQ23 | yuv420p | 页面预览 |
| `delivery` | H.264 CQ19 | yuv420p | 默认，同以前 |
| `master` | **ProRes 422 HQ** / DNxHR HQX / x265 10-bit | **yuv422p10le** | 归档、二次调色 |

`prores_ks` / `dnxhd` / `libx265` 已确认可用。master 档自动写 `.mov`、音频转 `pcm_s24le`、跳过 H.264 专用的 bitstream filter（用在 ProRes 上会直接报错）。

调用：`POST /v1/color/project-render` 传 `output_profile: "master"`。

---

## 7. 破坏性变更

### 7.1 配方数值会变（预期内）⚠️

`rgb_gains` 现在是**线性域增益**，包络从 `[0.88, 1.12]` 放宽到 `[0.80, 1.25]`。同一段素材重跑，几乎每个镜头的 `rgb_gains` 和 `exposure` 都会和以前不同。**历史 GradePlan 需要重新预览和批准。**

### 7.2 曝光 clamp 命中率上升

见第 4 节。这是正确行为，但报告里会多出很多 "asked for X EV, applied Y" 的提示。如果太吵，可以调 `EXPOSURE_CLAMP_EV`（`correction_engine.py`），但我建议先看看它标出来的是不是真的需要人看的镜头。

### 7.3 分析变慢

- 代表帧从 3 帧变成最多 9 帧
- 每个镜头分析两遍（黑边检测 + 加权测量）

长镜头多的片子分析时间大约翻倍。渲染时间不受影响（反而因为 D1 的浮点管线可能略慢一点，因为 lut3d 现在处理 float 而非 uint8——这一项我没有实测，需要你跑完看）。

### 7.4 master 档比 delivery 慢很多、文件大很多

ProRes 422 HQ 大约是 H.264 CQ19 的 8~15 倍体积，编码也是 CPU 的。别把它设成默认。

---

## 8. 验证步骤

```powershell
& C:\Users\Administrator\.conda\envs\localcolor\python.exe -m pytest -q

# 新增的两组
& ...\python.exe -m pytest tests/test_linear_wb_and_skin_exposure.py tests/test_render_precision.py -v
```

重跑那条 136 镜头素材后重点看：

| 位置 | 看什么 |
|---|---|
| `project_recipe` 结果 `.white_balance_diagnostics` | `adopted_consensus` 有多少（这些是以前完全没校正的镜头）、`flagged_different_light` 是不是真的不同光源 |
| `project_quality_report.json` | `within_group_continuity` 是否从 NEEDS_REVIEW 改善；WB 跳变数（现在单位是 mired） |
| 每个 shot 的 `base_correction.rationales` | 有没有出现 `skin_anchor` 的说明；`requested_exposure_ev` 被 clamp 的镜头 |
| `scene_groups[].diagnostics.mired_spread` | 组内色温一致性 |
| 成片天空/渐变/暗部 | D1 浮点管线 + dither 后 banding 是否改善 |

母版：

```powershell
curl -X POST http://127.0.0.1:8000/v1/color/project-render `
  -H "Content-Type: application/json" `
  -d '{\"project_job_id\":\"<id>\",\"output_profile\":\"master\"}'
# 产物：data\jobs\<id>\project_graded_master.mov
ffprobe -v error -show_entries stream=codec_name,pix_fmt -of csv=p=0 <该文件>
# 期望：prores / yuv422p10le
```

---

## 9. 这轮被实测推翻的判断

延续之前的习惯记下来：

1. **串联优于合成的理由我一开始写错了**。以为是保住 >1.0 的高光，实测发现 ffmpeg 的 lut3d 两种方式都会钳到定义域；真正的收益是避免网格重采样（9 倍误差差异）。注释已改。
2. **两个测试断言是我写错而不是代码错**：`decode_rec709(0.5)` 是 0.2596 我写了 <0.25；letterbox 那个测试该看均值和黑位裁切，中位数本来就不会变（60% 像素相同）。

---

## 10. 剩下的

原计划的 P0/P1/P2 到这里全部完成。还没做、也还没排期的：

- **任务取消 API**（上一轮暴露的缺口，日常会反复咬人）
- **`project_render` 进度反馈**（全程 progress=5）
- **磁盘保留策略**（你说自己清，但遮罩这类中间产物其实渲染完就能删）
- **GPU lut3d**（`libplacebo`），能同时解决 face-selective 的双 LUT 开销
- **B4**：饱和度指标仍是 HSV 的 S，暗部不稳定
- **banding 的真实检测**（需要测渲染后画面，不是 LUT 网格）
- **Golden Set**（ColorChecker/灰阶梯合成测试集）—— 我第一份报告里说这是"没有它改不动"的东西，到现在仍然没建

最后一项我建议优先，现在改动已经堆了五轮，缺乏 ground truth 的情况下每一轮都只能靠单元测试和你的目视判断。
