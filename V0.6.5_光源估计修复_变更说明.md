# V0.6.5 — 光源估计修复（含对上一轮结论的更正）

测试：**177 passed**（此前 172），6 个失败仍是沙箱环境。

---

## 0. 先更正我上一轮的结论

V0.6.4 的变更说明里我写：

> **3200K 钨丝灯：158.8 mired 误差。** 估计器三个候选分歧太大，直接放弃返回单位增益。

**这个结论是错的，因为测试场景选错了。**

我用 ColorChecker 给光源估计打分。但 ColorChecker 是 24 个**刻意饱和**的色块，它的平均值本来就不是中性的 —— 而灰世界假设的前提正是"场景平均反射率中性"。用它打分测的是**图卡违反假设的程度**，不是估计器的精度。

换成假设成立的场景（`neutral_scene`，构造时成对生成互补色偏，保证线性平均严格中性）重测，同一个估计器：

| 场景 | 旧实现误差 |
|---|---|
| D65 | 0.2 mired |
| **3200K 钨丝灯** | **0.0 mired** |
| 4300K | 0.1 mired |
| 9000K | 42.6 mired（放弃） |

**钨丝灯本来就是完美的。** 我上一轮基于错误的测量做出了错误的判断，并据此写进了 METRICS.md 和测试。

顺带说明：V0.6.4 里我把 `LINEAR_GAIN_MIN/MAX` 从 `[0.70, 1.45]` 放宽到 `[0.35, 4.50]` —— 那个改动**本身是对的**（旧范围只覆盖 4200–9800K，确实会截断），但它并没有解决我当时以为它要解决的问题。

`docs/METRICS.md` 现在专门有一节写"用哪张图测什么"，把这个坑记下来了。

---

## 1. 真正的缺陷：agreement 在增益空间衡量

上面唯一真实的失败是 **9000K 那条被放弃了**。查下来根因是候选一致性的度量空间错了。

实测三个候选的分歧程度：

| 光源 | 增益空间分歧 | agreement | mired 空间分歧 |
|---|---:|---:|---:|
| D65 | 0.112 | 0.591 | 13.3 |
| 3200K | 0.228 | 0.482 | 20.5 |
| 4300K | 0.135 | 0.694 | 19.3 |
| 9000K | 0.077 | 0.824 | 17.2 |

**在 mired 空间，四种光源的分歧都在 13–21，是同一量级。** 增益空间的分歧则被"光源离 D65 多远"结构性放大 —— 强色偏需要远离 1.0 的增益，同样的相对分歧变成更大的绝对数字。估计器是在为"光偏暖"受罚，而不是为"不确定"受罚。

更刺眼的是：钨丝灯的两个候选分别是 **2966K 和 2796K**（真值 3200K），估计器**本来就知道答案，然后把它扔了**。

### 改法

1. **候选融合与一致性判定改在 mired/Duv 空间**。取三个候选的 mired 中位数和 Duv 中位数，再转回增益。归一化尺度 60 mired（约 1/3 CTO）—— 公平场景实测分歧 1.3–12.5，违反假设的场景 19–45，区分度合适。

2. **不再丢弃估计**。原来 agreement 不达标就返回 `[1,1,1]` 并标记 `preserve_low_confidence`。这么做等于让"读不出光源的镜头"和"真正中性的镜头"变得无法区分 —— 而 C2 组内共识恰恰需要这个信息。现在估计**始终上报**，置信度如实标注，由 `correction_engine`（>0.60 才应用）和 C2 共识决定怎么用。安全性不变（应用侧的门槛没动），信息量增加。

### 结果

| 场景 | 旧 | 新 |
|---|---|---|
| D65 | 0.2 mired | **0.1 mired** |
| 3200K | 0.0 mired | **0.0 mired** |
| 4300K | 0.1 mired | **0.1 mired** |
| 9000K | **42.6 mired（放弃）** | **0.0 mired** |

---

## 2. 人脸特写白平衡：肤色排除

真实的缺口。人脸特写把主体当成光源 —— 肤色是橙的，灰世界据此认为光是橙的，于是把画面推冷。

**改法**：白平衡的三个候选改用"排除肤色像素"后的统计。曝光和裁切统计**不受影响**（它们本来就需要主体）。

实测（50% 肤色 + 50% 中性背景，D65）：

```
不排除肤色：5247K，误差 36.8 mired
  排除肤色：6504K，误差  0.0 mired，agreement 1.00
```

### 这里我又踩了一次

第一版直接用 `skin_mask_bgr(frame, face_only=False)` 排除所有肤色像素。结果**公平场景全崩了**：

| 场景 | 排除后 | 不排除 | 被判为"肤色"的像素 | 检出人脸 |
|---|---:|---:|---:|---:|
| neutral_scene_d65 | 11.2 mired | 0.1 mired | 33.4% | **0** |
| neutral_scene_4300k | **61.7 mired** | 0.1 mired | 78.0% | **0** |

**YCrCb 肤色分类器是一个宽色域范围，不是人脸检测器。** 在完全没有人的合成场景上它选中 16–90% 的像素（木头、泥土、任何暖色表面都符合），排除它们直接破坏了灰世界赖以成立的中性平均。

改成**以人脸检出为前提**（先跑 Haar，检出人脸才排除肤色），公平场景全部恢复到 ≤ 0.1 mired。

这一条已经作为断言写进测试：

```python
def test_skin_is_only_excluded_when_a_face_is_detected():
    """肤色分类器是色域范围不是人脸检测器 ..."""
```

---

## 3. 顺带修的一个 C2 设计缺口

跑测试时发现：一个与组内共识相差 **144 mired** 的镜头没有被标记，而是被直接拉到共识 —— 因为它的置信度 0.525，刚好低于 0.55 门槛，走了"不自信 → 采用共识"的分支。

这个逻辑有问题：**这么大的偏移，无论估计对错都该让人看一眼**。要么它真在不同光下（拉平是错的），要么估计坏了（那么这么大的自动修正也该复核）。

改成：**距离超限一律标记**，但只有"自信的离群镜头"保持不动（推定为真的不同光源）；不自信的仍采用共识，但带 `action: pulled_to_consensus` 出现在复核清单里。

---

## 4. 现在的 Golden Set 基线

| 项 | 结果 |
|---|---|
| 光源估计 @ D65（公平场景） | 0.1 mired ✅ |
| 光源估计 @ 3200K | 0.0 mired ✅ |
| 光源估计 @ 4300K | 0.1 mired ✅ |
| 光源估计 @ 9000K | 0.0 mired ✅ |
| 50% 肤色场景（排除肤色后） | 0.0 mired ✅ |
| **满屏肤色 @ D65** | **79 mired** ❌ 已知缺口 |
| ColorChecker @ D65 | 26.7 mired — 预期，非判据 |

**剩余唯一缺口：满屏肤色。** 画面里没有非肤色像素可用，排除机制无从下手。缓解是应用增益被 `[0.80, 1.25]` 限制（损害有界）+ C2 组内共识在真实剪辑中覆盖它。

Golden Set 现在有四类图，各自的用途和**不能用来测什么**都写进了 `docs/METRICS.md`。

---

## 5. 破坏性变更

1. **`selected_method` 的取值变了**：`ensemble_median` / `preserve_low_confidence` → `ensemble_mired` / `ensemble_mired_low_confidence` / `gray_world`。任何检查 `preserve_low_confidence` 的代码需要更新（`group_white_balance._shot_confidence` 已更新）。
2. **更多镜头会得到白平衡校正**：以前被放弃的现在有估计了。应用侧门槛没变（confidence > 0.60），但通过率会上升。
3. **有人脸的镜头白平衡估计会变**（现在排除肤色）。这是修复，但数值会不同。
4. 版本号 → **V0.6.5**。

---

## 6. 重跑时看什么

```powershell
& ...\python.exe scripts\build_golden_set.py     # 重新生成（新增 neutral_scene）
& ...\python.exe -m pytest tests/test_golden_set.py -v
& ...\python.exe -m pytest -q
```

同一条素材重跑后：

| 位置 | 期望 |
|---|---|
| `selected_method` 分布 | 不应再有 `preserve_low_confidence` |
| `white_balance_diagnostics.adopted_consensus` | 应该下降（更多镜头能自己读出光源） |
| `flagged_different_light` | 新增 `confident` 和 `action` 字段，看 `pulled_to_consensus` 那些是否合理 |
| 有人脸镜头的 `source_cct_kelvin` | 与上一版对比，应该更接近场景其他镜头 |
| `clipping_scenes` | P0-2 的高光保护，期望从 10 个降到 0~2 个 |
| `skin_tone_risks` | P0-1 的度量修复，期望从 40 降到个位数 |

---

## 7. 反思

这两轮里，**三次是测量方法本身出错**，而不是被测的代码出错：

1. 用 ΔE2000 判肤色安全 —— 被曝光污染（相关系数 0.813）
2. 用 ColorChecker 测光源估计 —— 图卡本身违反被测方法的假设
3. 用 YCrCb 肤色范围当人脸检测 —— 在无人场景选中 90% 像素

每一次都是靠**先量再改**发现的。如果按第一直觉直接改代码，第 2 条会让我去"修"一个根本没坏的钨丝灯路径，第 3 条会把一个 0.1 mired 的场景搞成 61.7 mired。

这也是为什么 `docs/METRICS.md` 现在不只记录指标定义，还记录**每个指标不能用来测什么**。
