# Local Color Service V0.5.2 完整操作手册

本文同时覆盖两条主流程：

1. 项目 Look / AdaInt 自动与半自动调色；
2. CanonCGT 参考图匹配。

后半部分覆盖管理员和开发者需要的安装、审批资产、QC、API、日志、备份和故障排查。

## 1. 能力边界

- 输入、工作和输出空间限定为 SDR Rec.709。
- 正式输出默认为 H.264、8-bit、`yuv420p`；它是交付文件，不是高质量中间母版。
- 系统会自动完成镜头切分、SceneGroup 建议、基础校正、受限 Shot Match、Look、LUT、时间线渲染和 QC。
- 黑场、近黑画面和淡入淡出候选默认保留，避免被误判为欠曝后抬亮。
- `NEEDS_REVIEW` 表示文件已经生成，但必须人工看过风险时间码后才能交付。
- 正式渲染必须使用已批准 GradePlan；每次修改都会撤销旧审批。

## 2. 安装与启动

### 2.1 推荐环境

- Windows 11；
- Python 3.10 Conda 环境 `localcolor`；
- NVIDIA GPU 与支持 NVENC 的驱动；
- 带 `lut3d`、`zscale`、`h264_nvenc` 和 `hevc_nvenc` 的 FFmpeg；
- 足够的 `data/jobs` 空间。批准时会冻结源媒体，优先创建硬链接，无法硬链接时复制文件。

不要直接在包含 NumPy 2.x 的通用 Python 环境中安装本项目。当前 SciPy、Colour 和部分模型依赖要求 `numpy>=1.26,<2`。

### 2.2 安装基础依赖

```powershell
conda create -n localcolor python=3.10 -y
conda activate localcolor
python -m pip install -r requirements.txt
```

CanonCGT 需要时再安装：

```powershell
powershell -ExecutionPolicy Bypass -File scripts\install_canoncgt.ps1
```

### 2.3 环境检查

```powershell
python scripts\doctor.py
```

必须重点确认：

- PyOpenColorIO 和 ACES Studio Config 可加载；
- FFmpeg/FFprobe 路径有效；
- `lut3d` 与 NVENC 可用；
- `data/jobs` 可写；
- 使用 AdaInt/CanonCGT 时 PyTorch、CUDA 和模型权重可用。

### 2.4 启动和停止

```powershell
start.bat
```

- 工作台：<http://127.0.0.1:8000/>
- Swagger：<http://127.0.0.1:8000/docs>
- 健康检查：<http://127.0.0.1:8000/health>

停止可在启动窗口按 `Ctrl+C`，或运行：

```powershell
stop.bat
```

## 3. 开始前的素材准备

推荐使用：

- 恒定帧率或元数据正确的 MP4/MOV；
- SDR Rec.709；
- 可访问的本地绝对路径；
- 音频轨道如需保留，应在分析前确认能被 FFprobe 识别。

参考图推荐：

- PNG 或高质量 JPEG；
- sRGB/Rec.709；
- 短边至少 512 像素；
- 不带水印、播放器控件、字幕和黑边；
- 内容、光线和主体尽量接近目标 SceneGroup。

## 4. 两条流程共用的前置步骤

1. 打开工作台，在素材路径处选择或填写视频。
2. 执行“镜头检测与分析”。
3. 检查镜头数、缩略图、时间线首尾和音频信息。
4. 选择项目 Look，生成项目配方。
5. 检查系统生成的 `shot_XXXX`、SceneGroup 和 Hero Shot。

新项目的 `shot_id` 与历史 `scene_id` 已分离：

```text
shot_id: shot_0001
legacy scene_id: scene_0001
scene_group_id: group_0001
```

黑场或淡变镜头会显示保留策略。不要为了让所有镜头“都有调整”而取消保留。

## 5. 主流程一：项目 Look / AdaInt

适合没有明确参考图、希望快速统一整片风格的项目。

### 5.1 自动流程

1. 生成项目配方时选择 `automatic` 工作流。
2. 系统对相邻镜头形成 SceneGroup，并为组内非 Hero Shot 生成受限 Shot Match。
3. Shot Match 只允许小幅曝光、白平衡、RGB gains 和饱和度修正；不会生成另一张无限制 AI LUT。
4. 生成整片预览。
5. 查看 QC 横幅和待复核数量。

### 5.2 半自动检查

在时间线逐组检查：

- Hero Shot 是否代表该组主要机位和光线；
- 同组白平衡是否一致；
- 往返切换机位是否被错误拆组；
- 黑场、字幕、片头和淡变是否保持原意；
- 人脸是否发生明显色相或亮度变化。

需要修正时：

- 使用“匹配 Hero Shot”重新生成受限 trim；
- 使用“恢复自动校正”撤销手动匹配；
- 调整单 Shot 的曝光、色温或 Look 强度；
- 必要时拆分或合并 SceneGroup；
- 保存修改后重新生成整片预览。

### 5.3 使用 AdaInt

1. 在作用范围中选择全部 SceneGroup 或单个 SceneGroup。
2. 选择 33 阶 LUT；只有明确需要更高精度时使用 65 阶。
3. 初始强度建议 0.50–0.75。
4. 生成 AdaInt，并始终查看整片预览，而不是只看 Hero Shot。
5. 重点检查肤色、天空渐变、暗部和高饱和物体。
6. 如果模型不可用，先修复模型环境；不要把统计回退误当成 AdaInt 模型结果。

## 6. 主流程二：CanonCGT 参考图匹配

适合已经有明确风格参考，需要按场景生成候选的项目。

### 6.1 参考图预检

1. 完成共用前置步骤并选择目标 SceneGroup；也可选择全视频。
2. 上传参考图并执行预检。
3. 检查分辨率、黑位、高光裁切、适配分数和警告。
4. 预检不通过时优先更换参考图，不要通过提高强度掩盖不匹配。

### 6.2 生成和选择候选

1. Provider 选择 CanonCGT。
2. 单组模式为该组 Hero Shot 生成 A/B/C；全视频模式逐 SceneGroup 独立生成。
3. 查看候选的 Fit Error、Technical Safety、Continuity、Reference Match 和 Total Score。
4. 先选安全候选，再看风格强度。最低 Fit Error 不等于最佳整片效果。
5. 选择候选后生成整片预览，检查场景边界和同组多镜头，不只检查参考帧。
6. 如出现 `fallback_used=true`，表示实际使用了统计回退；交付记录中必须保留这一事实。

### 6.3 微调原则

- 人脸异常：降低 Look 强度，或保留基础校正并旁路 Creative Transform。
- 组内不连续：先重新分组或匹配 Hero Shot，再调整创意 LUT。
- 黑场被抬亮：确认镜头的 `base_grade_policy=preserve`。
- 参考图只适合部分机位：拆分 SceneGroup，避免把一张 LUT 强套整组。

## 7. QC 状态与交付规则

项目报告包含五个关键字段：

| 字段 | 含义 |
|---|---|
| `render_integrity` | 文件可读、时长、帧率、帧数、音频和元数据 |
| `technical_color` | 裁切、LUT 稳定性和技术色彩安全 |
| `scene_continuity` | 相邻镜头亮度与白平衡跳变 |
| `skin_safety` | 人脸丢失、肤色色相或亮度风险 |
| `final_decision` | 上述状态的最严重结果 |

状态处理：

- `PASS`：可进入审批。
- `PASS_WITH_WARNINGS`：可以审批，但应阅读 warning 并记录原因。
- `NEEDS_REVIEW`：输出已生成；必须逐条查看 `review_items` 时间码，修复或人工签字后再交付。
- `FAIL`：技术失败，禁止交付。

不要只读取旧兼容字段 `passed`。自动审批应读取 `approval_recommended` 和 `final_decision`。

每个 `review_item` 包含类别、Shot、时间码；连续性项目还包含 `within_scene_group`，可区分组内问题和合理的场景边界变化。

## 8. 审批和资产冻结

点击“批准 GradePlan”后系统会：

1. 增加 revision；
2. 记录批准人和 UTC 时间；
3. 在批准 job 中创建 `approved_revision_XXXX`；
4. 冻结源媒体和已选择的 Creative Transform；
5. 为每个资产生成 SHA-256；
6. 写入 `recipe.json` 和 `manifest.json`；
7. 正式渲染前再次校验所有 hash。

典型目录：

```text
data/jobs/<approval_job_id>/
  project_recipe.json
  events.jsonl
  approved_revision_0002/
    recipe.json
    manifest.json
    media/source.mp4
    assets/group_0001_canoncgt.cube
```

源媒体通常以硬链接冻结，因此不要使用会破坏硬链接内容的工具原地覆盖文件。复制、移动整个批准目录是安全的；移动后应通过 API 或脚本复验。

复验 API：

```http
GET /v1/color/approval-bundle/verify?project_job_id=<approval_job_id>
```

只有返回 `valid=true` 才能认为批准包完整。

任何修改、重新选候选、重新匹配或改分组都会产生新 revision 并撤销旧审批。不得复用旧批准状态。

## 9. 正式渲染与产物

1. 确认 `final_decision` 不是 `FAIL`。
2. 对 `NEEDS_REVIEW` 完成人工复核。
3. 批准当前 revision。
4. 执行正式渲染。
5. 正式渲染会使用冻结源媒体，而不是页面上传入的其他同名文件。
6. 检查成片、`quality_report.json` 和 `project_quality_report.json`。

主要产物位于：

```text
data/jobs/<render_job_id>/
  project_graded_video.mp4
  project_recipe.json
  quality_report.json
  project_quality_report.json
  events.jsonl
  task.log
  shot_luts/
```

需要再剪辑或精调时，当前 H.264 输出只能作为参考或普通交付。高质量 Master 输出尚未纳入 V0.5.2。

## 10. API 使用流程

所有耗时 POST 接口返回 job ID。轮询：

```http
GET /v1/jobs/<job_id>
```

标准自动流程：

```text
POST /v1/color/scenes
POST /v1/color/project-recipe        workflow=automatic
POST /v1/color/project-render        preview=true
POST /v1/color/grade-plan/approve
GET  /v1/color/approval-bundle/verify
POST /v1/color/project-render        preview=false
```

CanonCGT 流程：

```text
POST /v1/color/scenes
POST /v1/color/project-recipe
POST /v1/color/reference-preflight
POST /v1/color/reference-candidates engine=canoncgt
POST /v1/color/reference-select
POST /v1/color/project-render preview=true
POST /v1/color/grade-plan/approve
POST /v1/color/project-render preview=false
```

正式渲染默认拒绝未批准配方。生产调用不要设置 `allow_unapproved=true`。

## 11. 日志、监控与清理

- `task.log`：适合人工阅读的生命周期日志。
- `events.jsonl`：机器可读事件，包含处理、资产冻结、LUT 完成、QC 决策和失败摘要。
- 服务日志：`data/logs` 或配置的日志目录。
- 数据库：`data/local_color.db` 或当前配置路径。

清理规则：

- 可清理未批准的预览 job 和时间线缓存；
- 不要删除仍被项目引用的 Scene Analysis job；
- 已批准项目应整体归档 `approved_revision_XXXX`；
- 删除批准包前必须确认没有交付或重渲染需求；
- 清理后抽样运行批准包复验。

## 12. 常见故障

### 12.1 `No module named colour` 或 `PyOpenColorIO`

进入正确的 `localcolor` 环境并运行：

```powershell
python -m pip install -r requirements.txt
```

### 12.2 `numpy.dtype size changed`

这是 NumPy/SciPy ABI 不兼容。确认：

```powershell
python -m pip install --force-reinstall "numpy>=1.26,<2" "scipy>=1.11"
```

不要在同一环境混装要求 NumPy 2.x 的其他视觉推理套件。

### 12.3 AdaInt 不可用

检查 `/v1/models/status`、PyTorch、CUDA、checkpoint 路径。基础流程仍可运行，但 AdaInt 测试会失败或跳过。

### 12.4 批准包校验失败

- 不要继续正式渲染；
- 检查 manifest 中报错的 `relative_path`；
- 从原批准 revision 恢复整个目录；
- 无法恢复时重新选择资产、重新批准，生成新 revision。

### 12.5 项目显示 `NEEDS_REVIEW`

这是人工复核门，不是渲染崩溃。打开 `project_quality_report.json` 的 `review_items`，按时间码检查并选择：修复、降低强度、重新分组或记录人工接受。

### 12.6 正式渲染提示源 hash 不匹配

当前源文件与 GradePlan 创建时的源媒体不同。恢复正确源文件或重新分析并生成新项目；不要绕过 hash。

## 13. 回归验证

基础回归：

```powershell
python -m pytest -q
```

如果仅 AdaInt 官方 checkpoint 测试失败，而基础、时间线、QC 和审批测试通过，先检查可选 PyTorch/CUDA 环境。发布前仍应在完整 `localcolor` 环境跑全量测试，并对一条带黑场、人物、渐变和多场景的视频执行两条主流程的真实验收。

## 14. 交付检查清单

- [ ] 镜头时间线无缝隙、无重叠；
- [ ] 黑场、片头、淡入淡出未被意外抬亮；
- [ ] SceneGroup 与 Hero Shot 合理；
- [ ] 组内亮度和白平衡连续；
- [ ] 人脸风险已逐条复核；
- [ ] CanonCGT/AdaInt 是否回退已明确记录；
- [ ] `final_decision` 已判读；
- [ ] 当前 revision 已批准；
- [ ] 批准包 `valid=true`；
- [ ] 成片音频、时长、帧率和画面已人工确认；
- [ ] 视频、recipe、manifest、QC 报告一并归档。
