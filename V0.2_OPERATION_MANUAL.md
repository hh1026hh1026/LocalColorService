# 本地智能调色服务 V0.2 操作手册

更新日期：2026-08-03

## 1. 当前运行环境

- 项目目录：`F:\LocalColorService`
- 服务地址：`http://127.0.0.1:8000`
- 中文测试页面：`http://127.0.0.1:8000/`
- API 文档：`http://127.0.0.1:8000/docs`
- Python：3.10.20，Conda 环境 `localcolor`
- 显卡：NVIDIA GeForce RTX 3090，24 GB 显存
- AdaInt 检查点：`models\adaint\AiLUT-FiveK-sRGB.pth`
- 当前 PyTorch：`2.11.0+cu128`；CUDA 可用，AdaInt 使用 RTX 3090 推理

输入素材目前按 SDR Rec.709 处理。不要直接输入未转换的 Log、PQ、HLG 或错误标记色彩空间的素材。

## 2. 启动和停止

双击项目根目录的 `start.bat`，或在 PowerShell 中运行：

```powershell
cd F:\LocalColorService
.\start.bat
```

启动成功后打开：

```text
http://127.0.0.1:8000/
```

命令窗口中按 `Ctrl+C` 停止服务。若修改过 Python 后端代码，必须重启服务；网页代码修改后刷新浏览器即可。

环境检查：

```powershell
& C:\Users\Administrator\.conda\envs\localcolor\python.exe scripts\doctor.py
```

## 3. 中文页面操作流程

### 3.1 选择素材

1. 在“选择待调色素材”中粘贴图片或视频的本机绝对路径。
2. 也可点“选择文件”，浏览器会把文件复制到 `data\uploads` 后填入服务端路径。
3. 快速测试可点“标准视频”或“标准图片”。
4. 视频建议先用 10～30 秒短片测试，再处理长片。

### 3.2 镜头级项目调色（推荐）

严格按按钮编号执行：

1. 选择检测方式。一般使用“自适应检测”；明显硬切、固定机位素材可试“内容变化检测”。
2. 检测阈值保持 `3.0` 起步。切得太碎就调高，漏切就调低。
3. 最短镜头帧数保持 `12` 起步。快速剪辑可降低，长镜头节目可提高。
4. 选择整体风格。页面会同步显示适用内容、核心特点和必须保护项。当前包含自然广播、明亮商业、柔和电影印片、克制青橙、暖调回忆、冷峻悬疑、赛事鲜明、舞台混光和日拍夜九套工程预设。
5. 点“1 检测并分析镜头”。右侧会显示镜头数量、时间范围、亮度、曝光建议和异常提示。
6. 点“2 生成项目配方”。此时会把镜头技术校正与整体风格分层保存。
7. 点“3 生成项目预览”。完成后右侧自动选择原片和最新镜头项目预览。
8. 用“擦除”或“并排”模式检查画面；播放、进度和倍速会同步。
9. 确认满意后点“4 正式渲染”。正式输出优先使用 NVENC，失败时自动回退到软件编码。

每次重新执行镜头分析后，都应重新生成项目配方。改变整体风格后也需再次生成项目配方。

AI LUT、参考图候选与 Look Package 当前是并行入口，不会自动叠加。详细关系和九套参数见 `LOOK_PACKAGE_REFERENCE.md`。

### 3.3 导出项目调色数据

生成项目配方后选择格式并点“导出配方”：

- CUBE：通用三维查找表。
- CC：单镜头 ASC-CDL；需要填写镜头编号，例如 `scene_0001`。
- CCC：多镜头 ASC-CDL 集合。
- CLF：可表达更完整处理的通用色彩变换文件。

导出路径会显示在右侧“任务结果”中，实际文件位于 `data\jobs\{job_id}`。

### 3.4 AdaInt 智能 LUT

1. 确认页面顶部显示“已检测到 NVIDIA 显卡”，模型状态显示“模型可用”。
2. “取样时间”留空时自动取视频中间帧；也可输入秒数。
3. 33 阶适合测试和常规输出，65 阶文件更大、处理更慢。
4. 点“生成 AdaInt LUT”。
5. 完成后点“预览 AdaInt 效果”，系统会将 LUT 套回完整素材并执行质量检查。
6. 右侧对比区自动载入原片和预览结果。

AdaInt 只分析代表帧并生成 LUT，不会逐帧运行模型，因此结果更稳定，也便于审计和复现。

### 3.5 参考图 A/B/C 候选

1. 在“参考图”路径框中填写参考图片或视频路径，或点“选择参考图”上传。
2. 测试阶段选择“统计匹配”，无需额外模型。
3. 点“生成 A / B / C 三套候选”。
4. 三个候选分别为：A 克制自然、B 均衡明亮、C 柔和电影感。
5. 在候选卡片中点“选择并预览”。系统会记录选择、复制选定 LUT、套回原片并显示对比。
6. “扩散模型”只有配置 `REFERENCE_LUT_COMMAND` 后才可运行；未配置时任务会明确失败，不会伪装为统计结果。

### 3.6 快速整片调色

展开“快速整片调色（兼容原有功能）”，可继续使用 V0.1 工作流：画面分析、手动调曝光/对比度/饱和度/色温、快速预览、正式渲染和导出 LUT。

## 4. RTX 3090 CUDA PyTorch wheel 手动下载

本机环境是 Python 3.10、Windows 64 位，因此 wheel 标签必须同时包含：

```text
cp310-cp310-win_amd64
```

推荐文件：

```text
torch-2.11.0+cu128-cp310-cp310-win_amd64.whl
```

官方目录：

```text
https://download.pytorch.org/whl/cu128/torch/
```

官方文件直链：

```text
https://download-r2.pytorch.org/whl/cu128/torch-2.11.0%2Bcu128-cp310-cp310-win_amd64.whl
```

文件约 2.75 GB。可直接用浏览器或下载工具下载。若用 PowerShell 并希望断点续传：

```powershell
curl.exe -L -C - --retry 20 --retry-delay 5 `
  -o "D:\Downloads\torch-2.11.0+cu128-cp310-cp310-win_amd64.whl" `
  "https://download-r2.pytorch.org/whl/cu128/torch-2.11.0%2Bcu128-cp310-cp310-win_amd64.whl"
```

先完成下载，再停止服务并替换当前 CPU 版：

```powershell
& C:\Users\Administrator\.conda\envs\localcolor\python.exe -m pip uninstall -y torch
& C:\Users\Administrator\.conda\envs\localcolor\python.exe -m pip install --no-deps "D:\Downloads\torch-2.11.0+cu128-cp310-cp310-win_amd64.whl"
```

验证：

```powershell
& C:\Users\Administrator\.conda\envs\localcolor\python.exe -c "import torch; print(torch.__version__); print(torch.version.cuda); print(torch.cuda.is_available()); print(torch.cuda.get_device_name(0))"
```

正确结果应包含：

```text
2.11.0+cu128
12.8
True
NVIDIA GeForce RTX 3090
```

RTX 3090 可运行 CUDA 12.8 wheel。本机驱动为 596.21，已经足够新。官方 wheel 自带运行时依赖，通常不需要另外安装完整 CUDA Toolkit；需要的是兼容的 NVIDIA 驱动。

本机已于 2026-08-03 完成安装和服务重启，页面“运行设备”现在显示 `NVIDIA GeForce RTX 3090`。

## 5. API 异步调用规则

创建类接口先返回 `job_id`，随后查询任务状态：

```powershell
$ColorApi = "http://127.0.0.1:8000"

function Wait-ColorJob([string]$JobId) {
    do {
        Start-Sleep -Milliseconds 400
        $job = Invoke-RestMethod "$ColorApi/v1/jobs/$JobId"
        Write-Host "$($job.job_type) $($job.status) $($job.progress)%"
    } while ($job.status -in @("pending", "processing"))
    if ($job.status -eq "failed") { throw $job.error_message }
    $job.result_data
}
```

镜头分析示例：

```powershell
$job = Invoke-RestMethod -Method Post -Uri "$ColorApi/v1/color/scenes" `
  -ContentType "application/json" `
  -Body (@{
    file_path = "F:\素材\input.mp4"
    detector = "adaptive"
    threshold = 3.0
    min_scene_len = 12
    analyze = $true
  } | ConvertTo-Json)

$result = Wait-ColorJob $job.job_id
```

通用 LUT 预览接口：

```powershell
$job = Invoke-RestMethod -Method Post -Uri "$ColorApi/v1/color/lut-render" `
  -ContentType "application/json" `
  -Body (@{
    input_path = "F:\素材\input.mp4"
    lut_path = "F:\LUT\look.cube"
    target_height = 720
    preview = $true
    split_screen = $false
  } | ConvertTo-Json)
```

## 6. V0.2 主要接口

- `GET /health`
- `GET /version`
- `GET /v1/looks`
- `GET /v1/models/status`
- `GET /v1/comparison/media`
- `POST /v1/color/scenes`
- `POST /v1/color/project-recipe`
- `POST /v1/color/project-render`
- `POST /v1/color/export`
- `POST /v1/color/adaint-lut`
- `POST /v1/color/reference-candidates`
- `POST /v1/color/reference-select`
- `POST /v1/color/lut-render`
- `GET /v1/jobs/{job_id}`
- `GET /v1/jobs/{job_id}/report`

## 7. 任务文件和排障

任务文件统一保存在：

```text
data\jobs\{job_id}\
```

常见产物包括镜头分析 JSON、项目配方、镜头 LUT、预览/正式视频、质量报告、AdaInt LUT、参考候选、选择记录以及导出的 CC/CCC/CLF/CUBE。

排障顺序：

1. 看页面右侧“任务结果”。
2. 点右上角“运行日志”。
3. 查看 `data\jobs\{job_id}\task.log` 和 `request.json`。
4. 运行 `scripts\doctor.py`。
5. 修改后端后只保留一个服务进程，避免多个 Worker 共同消费同一个 SQLite 任务队列。

镜头切分过多：提高阈值或最短镜头帧数。切分过少：降低阈值。正式输出未使用 NVENC：检查 FFmpeg 编码器和 NVIDIA 驱动，服务会在硬件编码不可用时回退到 `libx264`。
