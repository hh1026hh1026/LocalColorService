# 本地智能调色服务 V0.2 Review

Review 日期：2026-08-03

## 结论

V0.2 已达到“本机可视化功能测试 + 异步 API 服务”的阶段目标。后端能力已经完整接入中文 WEB 页面，用户无需手写请求即可完成镜头检测、项目配方、项目预览/正式渲染、格式导出、AdaInt LUT、参考图三候选、候选选择和 LUT 套用预览。

当前适合用于短片和真实素材样本测试，还不应按生产级多租户服务评估。用户已明确暂不处理 P0/P1 服务化事项，因此本次重点是能力完整性和测试可操作性。

## 本次新增与修正

### 中文 WEB 工作台

- 页面标题、说明、标签、按钮、状态、错误提示和对比素材名称均已中文化。
- 新增四步镜头项目工作流，并显示镜头任务与项目任务状态。
- 新增镜头结果表和整片亮度/裁切汇总。
- 新增 CUBE、CC、CCC、CLF 导出入口。
- 新增 AdaInt 模型/运行设备状态、LUT 生成和直接预览。
- 新增参考素材上传、统计/扩散引擎选择、A/B/C 候选卡和选择预览。
- 保留原有快速整片调色功能，收纳在折叠面板中。
- 对比播放器支持并排、擦除、同步播放、同步进度、倍速和 A 路声音。
- 增加静态资源版本号，避免浏览器继续使用旧版 JS/CSS 缓存。

### 后端闭环

- 新增 `POST /v1/color/lut-render`，可将任意已生成 CUBE 套回素材，生成预览或正式结果并执行 QC。
- AdaInt 和参考图候选因此不再只返回 LUT 文件，而是可以从 WEB 页面直接查看实际效果。
- 对比素材索引增加 `project_preview.mp4` 和 `project_graded_video.mp4`。
- 修复对比素材中文标签乱码，项目级输出可以自动与原片配对。
- 内置 Look 从三套扩展到九套，增加适用内容、核心特点、必须保护、工程说明和权威设计依据元数据。

## 实际验收结果

自动化测试：

```text
33 passed, 1 warning
```

唯一 warning 是现有 Starlette TestClient/httpx 弃用提示，不影响运行。

本机服务检查：

| 检查项 | 结果 |
|---|---|
| `/health` | 正常，版本 0.2.0 |
| RTX 3090 | 已识别 |
| AdaInt 检查点 | 可用 |
| PyTorch 运行设备 | `2.11.0+cu128`，NVIDIA GeForce RTX 3090 |
| 内置 Look | 9 套 |
| 中文首页 | HTTP 200 |
| OpenAPI 中的 LUT 渲染接口 | 存在 |

真实浏览器验收使用 `neutral_sample.mp4` 和 `sample_image.png`，完成以下链路：

| 页面操作 | 结果 |
|---|---|
| 页面初始化 | 服务、GPU、模型、PyTorch 设备、9 套 Look 均正确显示 |
| 镜头检测与分析 | 成功，识别 1 个镜头并填充表格 |
| 生成项目配方 | 成功，生成 1 个镜头配方 |
| 镜头项目预览 | 成功，质量检查通过并进入对比播放器 |
| 生成 AdaInt LUT | 成功 |
| AdaInt LUT 套用预览 | 成功，质量检查通过 |
| 参考图 A/B/C 候选 | 成功，返回 3 张候选卡 |
| 选择候选 B 并预览 | 成功，质量检查通过 |
| 切换“日拍夜”并生成项目配方/预览 | 成功，详情联动正确，质量检查通过 |
| 浏览器控制台 | 无 error/warn |

验收时发现本机曾同时运行 8000 和 8001 两个旧/新服务进程，它们共用同一个 SQLite 队列，旧 Worker 抢到了新任务并报“处理器不存在”。已停止旧的 8001 进程，只保留当前 8000 服务。后续测试也应遵守“一套数据目录只启动一个 Worker”。

## 架构判断

当前架构方向合理：模型仅在代表帧上理解或产生 LUT，完整视频由可重复、可审计的 OCIO/FFmpeg 链执行，结果经过 QC。技术校正、镜头匹配、创意 Look 和输出层保持分离，便于后续编辑、导出和追踪。

项目级渲染采用每镜头独立 LUT 和单次 FFmpeg 时间线处理，避免先生成多个有损片段再二次拼接。参考图流程默认使用可解释的统计匹配；扩散模型通过独立命令适配器隔离，不污染主环境。

## 已知边界

1. 输入仍假设 SDR Rec.709，尚无 Log/HDR 自动识别和 IDT。
2. CUDA PyTorch 已安装并由服务识别；后续仍需用长时间、多素材任务记录 GPU 推理耗时、显存峰值和稳定性。
3. 镜头匹配以逐镜头技术校正和连续性 QC 为主，尚无人物/机位聚类。
4. `skin_protection`、`gamut_protection` 已进入配方约束，但还不是语义遮罩驱动的局部保护。
5. 九套 Look 是保守的确定性工程预设，尚未建立跨体育、演出、企业片和剧情片的人工评分基准。
6. 扩散参考 LUT 只有外部命令适配器，本机未安装大模型及权重。
7. 数百镜头长片的 FFmpeg filter graph 长度、初始化时间和内存尚未做专项压力测试。
8. 页面上传会把素材复制进 `data\uploads`；超大视频测试更适合直接填写本机路径。
9. 当前服务无用户隔离、鉴权、配额、任务取消和并发治理；这是测试阶段的已接受限制。

## 后续建议

按当前“优先新增能力”的方向，建议顺序如下：

1. 先建立真实素材测试集与人工评分表：体育、演出 LED、企业访谈、室内混光、夜景、肤色特写各 10～20 段。
2. 对 RTX 3090 做 AdaInt 基准测试，记录不同素材、33/65 阶 LUT 的耗时和显存占用。
3. 增加项目配方编辑 UI：逐镜头启用/禁用、曝光微调、Look 强度和镜头复制。
4. 增加时间线缩略图与镜头定位，使多镜头项目更容易发现异常镜头。
5. 根据真实失败样本决定优先加入肤色/天空/草地/LED 保护，还是 SAM 2 遮罩。
6. 增加输入色彩空间选择和 Log/HDR 元数据检查，再扩展 ACES IDT。
7. 在接入外部调用方前补任务取消、并发上限、失败重试和数据清理策略。

最重要的下一步不是继续堆模型，而是用真实素材记录“输入特征—选择参数—输出 QC—人工评分”。这会直接告诉我们 V0.3 应投入镜头聚类、语义保护、参考模型还是色彩空间识别。
