# V0.6.1 — CanonCGT 流程卡住的真正原因 + 人脸遮罩性能

测试：**106 passed**（此前 86），6 个失败仍是沙箱环境（ffmpeg 4.4 无 `-fps_mode`、CanonCGT 测试调用 Windows conda python）。

---

## 1. 诊断结论：不是图片解析失败

日志和数据库里的事实：

| 任务 | 类型 | 状态 |
|---|---|---|
| `bc544849fe76` | project_render | **processing** — 09:51:17 开始，19 分钟后仍在跑 |
| `116ffe87bfc9` | reference_preflight (L1.png) | **pending** — 从未被取走 |

`data/jobs/116ffe87bfc9/task.log` 全文只有一行：

```
2026-08-06T10:06:58 job created
```

连 `processing` 都没有。**图片上传成功了（日志有 `Upload deduplicated: L1.png`），preflight 任务也创建成功了，只是排在一个 19 分钟的渲染后面永远没轮到。**

worker 是单线程的：

```python
while not self.stop_event.is_set():
    job = db_manager.fetch_next_pending_job()   # 一条队列，先到先服务
```

一个耗时 1 秒的 preflight（历史记录：`199852811154` 用了 0.9 秒）排在 15–17 分钟的渲染后面。而 API 只返回 `status: "pending"`，前端无法区分"在排队"和"失败了"——所以看起来像解析失败。

这正是我第一份分析里提到的"单 worker、不可取消"，只是没想到它会以这种形式先咬人。

---

## 2. 修复一：双车道队列

```python
LIGHT_JOB_TYPES = (
    "reference_preflight", "project_recipe", "grade_plan_revise",
    "grade_plan_approve", "timeline_adjust", "candidate_select",
    "export", "recipe", "lut",
)
```

- `JobWorker(lane="light")` 只取这些类型，`JobWorker(lane="heavy")` 取其余全部
- 未知类型一律归 heavy（保守默认，新任务不会误占快车道）
- `fetch_next_pending_job` 原本就用 `BEGIN IMMEDIATE` 做原子领取，注释里写着"so multiple workers cannot duplicate it" —— 本来就是按多 worker 设计的，只是没启用
- **启动恢复只由 heavy worker 执行**，否则两个线程可能把对方刚领走的任务重新标记为 pending

`app/main.py` 现在启动两个 worker。

### 排队可见性

`GET /v1/jobs/{id}` 新增三个字段：

```json
{
  "status": "pending",
  "lane": "light",
  "queue_position": 1,
  "blocked_by": null
}
```

`queue_position` 只统计**同车道**在前面的任务。preflight 排在渲染后面时，position 是 1 而不是 2 —— 因为渲染根本不挡它。等待就该显示成等待，不该和失败长得一样。

---

## 3. 修复二：人脸遮罩生成

### 先量，再改

我一开始猜是 Haar 人脸检测慢。实测 1080p 单帧：

| 环节 | 耗时 |
|---|---:|
| Haar detectMultiScale (720px) | 2.44 ms |
| skin_mask_bgr 全流程 | 12.14 ms |
| cv2 全分辨率解码 | 2.52 ms |
| **FFV1 1080p 三通道写入** | **42.14 ms** |
| FFV1 1080p 单通道写入 | 26.89 ms |
| FFV1 480×270 单通道写入 | **1.70 ms** |

**瓶颈是遮罩视频的写入，是解码的 17 倍**，而且写的是三通道——一个灰度遮罩被复制成了 BGR 三份。

这解释了耗时差异：

```
face-selective 渲染: 25f42d46a56f 15.7 min, c8e3ff0b8c9d 17.1 min
普通渲染:            2.0 - 4.2 min
```

遮罩 pass 约 6–7 分钟，加上渲染 pass 里**每个镜头跑两次 lut3d**（full 分支 + face 分支，且这台机器的 LUT 是 CPU 四面体插值），合计 15–17 分钟。

### 改动

1. **遮罩改单通道 + 1/4 分辨率**（`FACE_MASK_SCALE=0.25`）。遮罩本来就要经过 σ≈6 的高斯模糊，是低频信号，全分辨率不携带任何信息。滤镜图里再用 bilinear 放大回视频尺寸——`maskedmerge` 要求三路输入尺寸一致。
2. **不需要保护的镜头直接写黑帧**，跳过肤色检测。`render_face_selective_timeline` 现在比较 `full_lut` 和 `face_lut` 是否相同，只把真正用到保护的时间段传给遮罩生成器。
3. **人脸检测按间隔复用检测框**（`FACE_MASK_DETECT_INTERVAL=3`）。人脸在 1/10 秒内不会明显移动，且已有的时间平滑会吸收残差。`skin_mask_bgr` 新增 `reuse_boxes` 参数。

### 验证

冒烟测试（640×360，100 帧）：

```
mask generated in 0.10s  size=0.02 MB
mask stream: 160,90,gray          ← 1/4 分辨率，单通道
render OK, output: 640,360,100 frames
```

遮罩路由正确性（构造左半白、右半黑的遮罩，face LUT = ×0.40，full LUT = 恒等）：

```
left  half (mask=white -> face LUT x0.40): output/source = 0.400
right half (mask=black -> full LUT x1.00): output/source = 0.999
```

降分辨率再放大没有破坏 maskedmerge 的路由。

### 预期收益

遮罩 pass 的写入成本按实测比例下降约 25 倍（42.14 → 1.70 ms/帧）。

**但要说清楚**：这个倍数是在随机噪声上测的，那是 FFV1 的最坏情况（不可压缩）。真实肤色遮罩大部分是纯黑，压缩率高得多，所以实际节省会小于 25 倍。方向和量级可信，具体数字要你跑完才知道。

**没有优化的部分**：渲染 pass 里的双 lut3d 仍然存在，那是 face-selective 方案的固有成本。真正要解决它需要 GPU lut3d（`libplacebo`），属于 D1 的范畴。

---

## 4. 你现在该做什么

### 重启服务

`bc544849fe76` 那个渲染现在仍是 `processing`。重启后：

- `recover_interrupted_jobs` 会把它重新排进 heavy 车道（它的 `request.json` 还在，可恢复），重跑一次——这次会快一些
- **`116ffe87bfc9` 那个 preflight 会立刻在 light 车道跑掉**，不再等渲染

如果你不想再跑那个渲染，重启前把它的数据库记录删掉即可（目前没有取消 API——这是个真实缺口，见下）。

### 验证队列修复

```powershell
# 起一个长渲染，同时传参考图做 preflight，两者应该并行
# 查任务状态时现在能看到排队位置：
curl http://127.0.0.1:8000/v1/jobs/<job_id>
# -> {"status":"pending","lane":"light","queue_position":1,"blocked_by":null}
```

### 验证遮罩

跑一次 face-selective 渲染后：

```powershell
ffprobe -v error -select_streams v -show_entries stream=width,height,pix_fmt `
  -of csv=p=0 data\jobs\<job_id>\face_protection_mask.avi
# 期望：480,270,gray（1080p 源）而不是 1920,1080,yuv420p/bgr
```

对比总耗时和 `face_protection_mask.avi` 的大小。

---

## 5. 顺带暴露出来的缺口

- **没有任务取消 API。** 一个跑错的 15 分钟渲染只能等它结束或重启服务。这在日常使用里会反复咬人，建议尽快补一个 `DELETE /v1/jobs/{id}`（对 processing 的任务需要能杀掉 ffmpeg 子进程）。
- **`project_render` 没有进度反馈。** 全程 `progress=5`，用户看不到到了哪一步。事件流 `events.jsonl` 里其实有 `render.luts_ready` / `render.qc_completed`，可以映射成百分比。
- 磁盘：`data/jobs` 现在有 431 个任务目录。那个 153 MB 的遮罩就是这么攒出来的——遮罩其实是中间产物，渲染完就可以删。

---

## 6. 下一步

原计划的 P1 队列里还剩：

| # | 项 | 预期收益 |
|---|---|---|
| C2 | 低置信度镜头继承组内共识白点 | 直接打组内 WB 跳变；SceneGroup V2 已经提供了 `source_cct_kelvin` 作为正确度量，实现起来比之前干净 |
| A3 | 统计改线性域 | 灰世界的理论前提就是线性光；做完后 `WB_GAIN_LINEAR_EXPONENT` 应设回 1.0 |
| C1 | 肤色锚点驱动曝光 | 主观提升最大，但依赖 A3 才能算出真实 EV |
| C3 | 代表帧空间加权 + 自适应帧数 | 成本低，直接减少大面积天空/黑边对统计的污染 |
| D1/D2 | 渲染链浮点中间 + 10-bit 母版 | 消除 banding；顺带可能让 GPU lut3d 变得可行 |

A3 → C1 是有依赖关系的一组，建议一起做。C2 可以独立先做。

你先测队列和遮罩，然后告诉我从哪一项继续。
