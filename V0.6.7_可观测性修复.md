# V0.6.7 — 把"看不见"的问题修掉

测试：**205 passed**（此前 184），6 个失败仍是沙箱环境。

这轮不改画面，改的是**为什么上一轮的 bug 藏了那么久**。

---

## 背景

上一轮发现人脸保护从 V0.6.2 起就静默失效了，四次渲染全部退回普通时间线，只在日志里留下一行 WARNING。同时 `project_render` 全程报 5% 进度，6.5 分钟看起来和卡死一模一样。

两个问题的共同点：**系统知道出了什么事，但没有把它说清楚。**

---

## 一、区分程序 bug 与环境降级

出问题的那段：

```python
except Exception as exc:
    logger.warning(f"Face-selective render unavailable; using normal timeline render: {exc}")
```

这个 `except` 同时处理两类完全不同的事：

| 类型 | 例子 | 合理反应 |
|---|---|---|
| **环境限制** | 没有 NVENC 编码器、文件被占用、GPU 忙 | 静默降级，WARNING 即可 |
| **程序缺陷** | `TypeError: expected str, not list` | **绝不该静默** —— 接口两边对不上，不会自己好 |

V0.6.2 的链式 LUT 改动让 `face_specs` 里传了 list，`Path(list)` 抛 TypeError，被当成"环境限制"处理掉了。

### 改法

新增 `color_core/degradation.py`：

```python
PROGRAMMING_ERRORS = (TypeError, AttributeError, KeyError, IndexError,
                      NameError, UnboundLocalError, AssertionError)
```

- **缺陷** → ERROR 级别 + 完整 traceback + 明确措辞（"fell back because of a DEFECT... the feature is silently not running"）
- **环境** → WARNING，不带 traceback

同时写进任务产物：`safety_fallbacks` 里带 `cause: defect|environment`，事件流里多一条 `render.degraded`。这样即使没人看日志，QC 报告里也能看到。

已接入：人脸选择性渲染、DINOv2 场景 embedding（那里原来是裸 `pass`，连日志都没有）。

顺带修了个相关问题：那个 `except Exception` 会把 `JobCancelled` 也吞掉，导致取消任务时反而触发降级重渲染。现在先 `except JobCancelled: raise`。

---

## 二、渲染进度

`project_render` 只在被领取时写 5、结束时写 100，中间 6 分钟没有任何更新。

### 改法

`color_core/render_progress.py` —— 解析 FFmpeg 的 `-progress` 输出。

用**文件**而不是管道：渲染的 stdout/stderr 已经被可取消的 runner 捕获了，再插一路管道有死锁风险，而且进程被 kill 时不好收尾。写文件 + 轮询线程没有这些问题。

阶段权重：

```
 5 → 领取
 8–35 → 逐镜头烘 LUT（按镜头数线性推进）
35–92 → 渲染（由 FFmpeg 的 frame= 实时驱动）
92–98 → 渲染 QC + 项目 QC
   100 → 完成
```

实测（8 秒素材，veryslow 编码）采集到 0.01 → 0.56 → 0.79 的连续进度点。

配套：
- `db_manager.update_job_progress()` —— 只前进不后退，上限 99（100 留给完成），且只作用于 `processing` 状态的任务
- 前端加载遮罩现在显示进度条：`████████░░░░░░░░░░░░ 42%`

### 为什么进度只前进不后退

FFmpeg 的多次编码器回退（nvenc 失败 → libx264 重试）会让 frame 计数归零重来。允许后退的话，用户会看到进度条倒回去，比不动更让人困惑。

---

## 三、这轮新增的测试

`tests/test_degradation_and_progress.py`（21 项），其中：

```python
def test_the_exact_production_failure_is_classified_as_a_defect():
    """人脸渲染那次回归，原样。"""
    error = TypeError("expected str, bytes or os.PathLike object, not list")
    assert describe_degradation("Face-selective render", error)["cause"] == "defect"
```

以及：进度不能倒退、完成前不能到 100、非运行任务的进度更新被忽略、回调抛异常不能弄挂渲染、进度文件用完清理。

---

## 破坏性变更

无。都是可观测性增强，没有改变画面或配方。

唯一行为变化：**取消一个正在做人脸选择性渲染的任务时，现在会正确终止**，而不是先触发一次降级重渲染。

---

## 重跑时看什么

```
日志里如果出现 "fell back because of a DEFECT" —— 那是需要修代码的，不是环境问题
project_quality_report.json → safety_fallbacks[].cause
events.jsonl → render.degraded

界面加载遮罩应显示滚动的进度条，而不是固定 5%
```

---

## 剩下的

按证据排序，还没做的：

| 项 | 证据 |
|---|---|
| **满屏肤色白平衡** | Golden Set 量化：79 mired。需要肤色先验或 white-patch 融合 |
| **仍有 2 个组连邻组都读不出光源** | 上一轮实测。可以扩大继承范围（跨两跳）或用全片中位数兜底 |
| **B4：饱和度用 HSV 的 S** | 暗部不稳定，喂给 `grade_decision.creative_risk` 和饱和度补偿 |
| **banding 真实检测** | 目前 `max_curvature` 已证明无区分力，需要测渲染后画面 |
| **GPU lut3d（libplacebo）** | 能同时解决人脸选择性渲染的双 LUT 开销 |
| **多人协同评价** | 客观基线已就位，可以开始了 |

我建议下一步是**多人评价**而不是继续修。理由：这轮之后客观指标该修的都修了（高光 0、肤色度量正确、连续性口径正确、人脸保护真的在跑），剩下的四项要么是已知能力上限（满屏肤色），要么需要主观判断来定优先级（banding 到底有多明显、饱和度不稳有没有实际影响）。人评能告诉我们该往哪使劲。
