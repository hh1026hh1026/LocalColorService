# V0.6.3 运行结果分析与下一步建议

基于 `data/jobs` 里 V0.6.2/V0.6.3 之后的真实任务产物。

---

## 0. 先说一个方法论问题（影响下面所有结论的读法）

我在 V0.6 和 V0.6.2 里**同时修改了算法和衡量算法的度量**：

| 指标 | 改前定义 | 改后定义 |
|---|---|---|
| 白平衡跳变 | `abs(gain_b - gain_r) 差 × 50 > 12` | `mired 差 > 25` |
| 肤色风险 | HSV 色相差 > 0.08 或亮度差 > 0.18 | ΔE2000 > 3.0 |

所以"白平衡跳变 19 → 3"、"肤色风险 46 → 40"这类对比**是用不同的尺子量同一批画面，不能作为改进证据**。这是我的失误。

下面只用**定义没变的指标**做对比，并明确标注哪些是可比的。

另外三个任务的配置本身也不同：

| 任务 | 版本标志 | SceneGroup 带创意变换 | 耗时 |
|---|---|---|---|
| `bc544849fe76` | `exponent=2.2`（A3 前） | 22/22 组（AdaInt） | 77.6 min |
| `f15769b41639` | `exponent=1.0`（A3 后） | **0/27 组** | 4.1 min |
| `69591b908a00` | 同上，同一项目重跑 | 0/27 组 | 7.7 min |

**新的两次都没有应用任何创意变换**，只有项目 Look。所以渲染耗时的 77.6 → 4.1 min 不能完全归给遮罩优化。要公平比较，需要再跑一次带 AdaInt 的。

---

## 1. 确认有效的部分

### LUT 警告归零（可比，定义未变的部分）

```
[technical] count=60  with_warnings=0  failed=0
[combined]  count=76  with_warnings=0  failed=0
```

分层 QC 后没有任何 LUT 触发警告。对照最初报告里的"48 个警告、其中 46 个超范围采样 + 34 个中性轴分离"，这批系统性误报确实消除了。

⚠️ 但要留个心眼：**技术层和创意层双双 0 警告，也可能意味着阈值现在太松了**。当前只有中性轴漂移（technical）、单调性、肤色旋转三项在起作用，曲率检查我在 V0.6 已经因为无法标定而移除。这个"干净"需要用真正有问题的素材验证过才算数。

### 组内白平衡一致性

3 个白平衡跳变里，2 个跨组（正常剪辑点），只有 1 个组内：

```
00:00:15.233  shot_0014→shot_0015  Δ=29.8 mired  组内=True
```

阈值是 25 mired，这个 29.8 只是勉强超线。**组内白平衡基本一致了**，C2 起了作用。（但如前所述，不能和改前的"19 个"直接比数字。）

### 亮度跳变全部在组边界

13 个亮度跳变，抽查的 6 个 `within_scene_group` 全是 `False` —— 都是剪辑点，不是缺陷。

### 队列与取消

`reference_preflight` 现在 0.10 min 完成，不再被渲染堵住。V0.6.1 的双车道有效。

---

## 2. 问题一：肤色 QC 的度量被曝光污染 🔴

40 个肤色风险，ΔE2000 中位数 **8.61**、最大 **25.34**。看上去很严重。但：

```
ΔE2000 与 |EV| 的相关系数 = 0.813

|EV| >= 0.4 的镜头 (20 个)：ΔE2000 中位 12.32
|EV| <  0.1 的镜头 (11 个)：ΔE2000 中位  4.89
```

排名最高的 12 个全部是 `EV=+0.80`（撞上安全上限）的镜头。

**原因**：ΔE2000 包含 ΔL\*。一个 +0.8 EV 的提亮必然大幅改变肤色的 L\*，于是 ΔE2000 很大 —— 但那正是这次调色的**意图**，不是缺陷。

这是我在 V0.6 引入的缺陷：把肤色判据从 HSV 色相换成 ΔE2000，**色彩空间改对了，但用错了分量**。调色师关心的是"肤色有没有偏品红/偏绿"，不是"肤色有没有变亮"。

**修法**：把 ΔE2000 分解成 ΔL\* / ΔC\* / ΔH\*，主判据放在 **ΔH（色相旋转）和 ΔC（彩度）**，ΔL 只作参考记录。预计 40 个会降到个位数，而且每一个都是真的。

---

## 3. 问题二：高光被推爆 🔴

| shot | 源片高光裁切 | 成片高光裁切 | 源片中位亮度 | EV |
|---|---:|---:|---:|---:|
| shot_0080 | **0.0000** | **0.2171** | 0.2328 | +0.800 |
| shot_0018 | 0.0053 | 0.1154 | 0.2321 | +0.800 |
| shot_0017 | 0.0042 | 0.1097 | 0.2249 | +0.770 |
| shot_0092 | 0.0003 | 0.0919 | 0.4988 | +0.713 |
| shot_0077 | 0.0023 | 0.0901 | 0.4708 | +0.606 |
| …共 10 个 | | | | |

```
源片全部 136 镜头：超 6% 高光裁切的有 0 个，最大 2.47%
成片：超 6% 的有 10 个，最高 21.7%
```

**源片一个都没爆，成片爆了 10 个。** shot_0080 从 0.00% 涨到 21.7%。

这是 C1 的直接副作用：曝光量改用线性域真实 EV 后，提亮幅度比旧公式大得多（21 个镜头撞上 ±0.8 EV 上限，改前是 0 个），而**提亮路径上没有任何高光保护**。

注意 shot_0077（中位亮度 0.4708）和 shot_0092（0.4988）——这两个的画面中位亮度落在"正常区间"，旧的 `_exposure_from_frame` 会返回 0 EV。它们被提亮是因为**肤色锚点**判定人脸偏暗。C1 的判断本身是对的（画面不暗但人暗），但整帧抬 +0.6~0.7 EV 去救脸，代价是天空/窗户全爆。

调色师在这种情况下不会推全局曝光，而是配合高光滚降，或者做亮度限定的提亮。

**修法**（配方里 `highlight_rolloff` / `highlight_softness` 字段早就有，`ocio_manager` 也实现了，但自动校正从来不设置它们）：

1. 提亮时按提亮量自动加入高光滚降
2. 用源片的 `linear_p95` 预测提亮后的裁切比例，超限就回退 EV
   （线性增益 g = 2^EV，裁切比例 ≈ 亮度 > 1/g 的像素占比；`linear_p95` 已经在报告里）
3. 两者结合：先滚降，仍超限再压 EV，并在 rationale 里说明

---

## 4. 问题三：QC 判决被噪声主导

当前 `final_decision = NEEDS_REVIEW`，由三项驱动：

| 分项 | 状态 | 实际驱动因素 | 是否真问题 |
|---|---|---|---|
| `base_correction` | NEEDS_REVIEW | 10 个高光裁切 | ✅ 真的 |
| `within_group_continuity` | NEEDS_REVIEW | 1 个 29.8 mired（阈值 25） | ⚠️ 勉强超线 |
| `skin_safety` | NEEDS_REVIEW | 40 个，大部分是度量假象 | ❌ 假的 |

修掉问题一之后，`skin_safety` 应该会显著收敛；那时 `NEEDS_REVIEW` 才真正指向需要人看的东西。

另外 13 个亮度跳变**全部在组边界**，它们让 `scene_continuity` 变成 `PASS_WITH_WARNINGS` 是合理的，但都进了 `review_items` 列表——13 条剪辑点混在复核清单里会稀释真正要看的项。建议跨组跳变单独成一个分区，或默认折叠。

---

## 5. 一个次要观察

`f15769b41639` 和 `69591b908a00` 是**完全相同的请求**（同项目、同参数、同 `delivery` 档），产出的 QC 报告每一项数值都一模一样，但耗时 **4.10 min vs 7.72 min**，差 88%。

不影响正确性（结果确定性是好事），但说明渲染耗时受机器状态影响很大。如果要做性能基线，需要控制变量重复测。

---

## 6. 下一步建议（按优先级）

### P0-1 修复肤色 QC 度量（半天）

ΔE2000 分解为 ΔL\*/ΔC\*/ΔH\*，主判据用 ΔH + ΔC。

**验收**：同一条素材，肤色风险从 40 降到个位数，且抽查剩下的每一个都能在画面上看出色偏。

### P0-2 曝光的高光保护（1 天）

自动高光滚降 + 基于 `linear_p95` 的 EV 上限回退。

**验收**：成片超 6% 高光裁切的镜头数回到 0~2 个，同时肤色仍在目标带内（不能靠放弃提亮来达标——要看 `exposure_anchor=skin_anchor` 的镜头肤色最终落点）。

### P0-3 冻结指标定义 + 建 Golden Set（1~2 天）

这是我第一份报告就提、到现在第七轮仍然没建的东西，而这轮的方法论问题（改算法同时改尺子）正是它缺席的直接后果。

内容：
- 一份**冻结的指标定义文档**，此后改算法不改度量；确需改度量时，新旧两套并行输出一段时间
- 合成 Golden Set：24 色 ColorChecker + 21 级灰阶 + 已知 CCT 下的多种肤色样本，16-bit PNG
- 量化验收：中性轴保持、白点准确度（ΔCCT/ΔDuv）、肤色 ΔH/ΔC、往返精度

**没有这个，"主观看起来还行"就是我们唯一的证据**，而你已经提到需要多人协同评价——Golden Set 正好可以把客观部分先固定下来，让人的评价集中在真正主观的地方。

### P1 QC 复核清单分区（半天）

跨组跳变与组内问题分开呈现，避免 13 条剪辑点稀释真正要看的项。

### P1 补一次带 AdaInt 的渲染做性能基线（半小时）

当前 4.1 min 那次没有创意变换，和 77.6 min 那次不可比。跑一次同配置的才能确认遮罩优化和链式 LUT 的实际收益。

### P2 组内白平衡阈值复核

25 mired 可能偏紧（唯一的组内跳变是 29.8）。等 Golden Set 建起来后用数据定，不要凭感觉调。

---

## 7. 关于多人协同评价

你说这个放后续，我同意——但建议**先做 P0-3**。原因：盲测的成本很高（要组织人、要随机化、要统计显著），如果客观指标里还藏着高光爆掉、肤色误报这类问题，人会把注意力浪费在这些上面，反馈质量会很差。

顺序上：先把 P0-1/P0-2 修掉、P0-3 的客观基线立起来，再做人评，那时人评才能聚焦在"好不好看"而不是"这里怎么爆了"。
