# LocalColorService 调色准确性改造建议

> 目标：不增加新模型，通过修正色彩管理链路和校正算子，让现有两条流程调色更准确。
> 范围：代码级审查 `color_core/` 全部算法模块 + `workers/job_worker.py` + 渲染链。
> 结论前置：当前准确性的主要损失**不在算法策略层，在色彩管理层**。有三处是可证伪的色彩学错误。

---

## 0. 一句话总结

系统的工程骨架（审批、时间线、QC、Provider 抽象）已经比算法本身成熟。
现在的画面误差主要来自三件事：

1. **分析用的像素本身就是错的** —— 360p 代理丢色彩元数据，帧缓存二次 JPEG 压缩；
2. **白平衡用错了数学** —— 线性域的通道增益被当成 CDL slope 应用在 ACEScct 对数空间；
3. **决策目标不像调色师** —— 用"全画面亮度中位数 → 0.40"驱动曝光，而不是用肤色/高光锚点。

修掉 1 和 2 是 1–2 天的工作量，但对画面准确性的提升会超过后面所有改动的总和。

---

# 第一部分：色彩管理链路（根因层）

## A1 — 分析代理丢失色彩元数据，存在 BT.601/709 矩阵误判风险 🔴 P0

**位置**：`color_core/frame_sampler.py::ensure_analysis_proxy`

```python
[ffmpeg, "-y", "-hwaccel", "cuda", "-i", str(source),
 "-an", "-vf", f"scale_cuda=-2:{target_height}",
 "-c:v", "h264_nvenc", "-preset", "p2", "-cq", "28", "-b:v", "0", str(output)]
```

三个问题叠加：

| 问题 | 后果 |
|---|---|
| 无 `-colorspace/-color_primaries/-color_trc` | 360p ≤ 576p，矩阵标记缺失时大量解码路径按 **BT.601** 解 YCbCr→RGB。源是 BT.709。 |
| 无 `-color_range` | full/limited 误判会整体压黑或抬黑 |
| `-cq 28` + 4:2:0 | 色度被重压，恰好是白平衡估计最敏感的通道 |

BT.601 vs BT.709 矩阵错配的典型表现是**绿–洋红方向的系统性色相偏移**，饱和度也会偏。而 `analyze_image_frames` 的灰世界估计正是在通道均值比上工作的 —— 这个偏移会被原样翻译成一个错误的 WB 增益，然后写进每个镜头的配方。

报告里源片 **33.35% 黑位裁切** 这个数字也很可疑：一部正常成片不该有三分之一像素低于 1% 亮度。除去 letterbox，range 误判是另一个高度可能的解释。

**修正**

```python
# 1) 从 probe 继承源标记；缺失时按分辨率兜底（≥720p → bt709）
"-colorspace", src_matrix, "-color_primaries", src_primaries,
"-color_trc", src_trc, "-color_range", src_range,
# 2) 质量：分析代理不该省这点码率
"-cq", "20",
```

更稳妥的做法：**分析不走 H.264 代理**。代理只用于时间线缩略图和预览播放；统计和模型采样直接从原片 `-vf scale` 出 PNG（`sample_frames_at` 本来就有批量解码路径，成本可控）。

**验收**：拿一段带 ColorChecker 的 1080p 素材，比较"原片直接统计"与"经代理统计"的 24 色块 ΔE2000。目标 < 1.0，当前预计在 3–6。

---

## A2 — 代表帧缓存为 JPEG q92 4:2:0 🟠 P0

**位置**：`frame_sampler.py::sample_frames_at`

```python
cv2.imwrite(str(cache_dir / f"frame_{i:09d}.jpg"), image, [cv2.IMWRITE_JPEG_QUALITY, 92])
```

OpenCV 的 JPEG 编码默认 4:2:0 子采样。用于亮度统计影响有限，但这批帧同时是 **CanonCGT LUT 拟合的 source/target 配对输入**（`lut_fitter.fit_lut_from_pairs`）。多项式回归里的 `r*g`、`r*b`、`g*b` 交叉项会直接学到色度子采样在边缘产生的伪影，再被 `gaussian_filter` 抹成一个"平滑的错误"。

**修正**：缓存改 PNG；若担心体积，用 `[cv2.IMWRITE_JPEG_QUALITY, 100, cv2.IMWRITE_JPEG_SAMPLING_FACTOR, cv2.IMWRITE_JPEG_SAMPLING_FACTOR_444]`。LUT 拟合路径**必须**用无损帧。

---

## A3 — 全部统计量算在 gamma 编码值上 🟠 P1

**位置**：`image_analyzer.py::analyze_image_frames`

```python
img_f = img.astype(np.float32) / 255.0          # 仍是 Rec.709 OETF 编码值
y_weights = colour.RGB_COLOURSPACES["ITU-R BT.709"].matrix_RGB_to_XYZ[1]
lum = y_weights[0]*r + y_weights[1]*g + y_weights[2]*b
```

把 **线性光的 XYZ 转换矩阵**用在 **gamma 编码值**上。得到的是 luma（Y'），不是 luminance（Y）。两个具体后果：

**(a) 曝光启发式失去物理含义。**
`correction_engine` 里 `needed_ev = (0.40 - lum_med) * 2.0` —— 在 gamma 域，中位数从 0.25 到 0.40 对应的实际曝光变化，与从 0.45 到 0.60 完全不同（前者约 +1.4 EV，后者约 +0.9 EV），但代码用同一个线性系数。所以同一段代码对暗片和亮片的行为是不一致的。

**(b) 灰世界必须在线性光下做。**
Gray World / Shades-of-Gray 的理论前提是「场景平均反射率中性」，这是**线性光**命题。在 gamma 域求通道均值比，暗部像素被 OETF 放大了权重，会系统性低估偏色量并把估计偏向暗部光源。

**修正**

```python
def _to_linear(img_u8, trc="bt709"):
    x = img_u8.astype(np.float32) / 255.0
    # BT.1886 / sRGB EOTF，按源标记选择
    return np.where(x <= 0.081, x / 4.5, ((x + 0.099) / 1.099) ** (1/0.45))
```

统计（亮度、通道均值、WB 候选、裁切）全部在线性域计算；UI 展示指标再转回显示域即可。裁切检测保留在编码域（0.99 阈值对应的是码值，本来就该在编码域判）。

---

# 第二部分：校正算子的色彩学正确性

## B1 — 白平衡增益作为 CDL slope 应用在对数空间 🔴🔴 P0 最高优先级

**位置**：`ocio_manager.py::build_grade_group`

```python
group.appendTransform(ocio.ColorSpaceTransform(src=reference_space, dst=recipe.working_color_space))  # → ACEScct (log)
...
slope = [1.0 + (effective_wb[i] * recipe.gain[i] - 1.0) * strength for i in range(3)]
cdl = ocio.CDLTransform()
cdl.setSlope(slope)          # ← 在 ACEScct 对数域施加逐通道乘法
```

`rgb_gains` 来自灰世界，语义是**线性域的通道增益**。但它被施加在 ACEScct（对数编码）上。

数学上：

- 线性域增益 g 等价于对数域的 **加法 offset**：`log(g·x) = log g + log x`
- 对数域的 **乘法 slope** 等价于线性域的 **逐通道 gamma**（这是 printer-light 风格的操作）

**所以当前代码做的不是白平衡，是逐通道 gamma。**

具体表现：中性轴上 `r = g = b = L`，输出为 `(s_r·L, s_g·L, s_b·L)`。三个 slope 不同 → 偏离量随 L 增大而增大 → **暗部和亮部朝相反方向偏色，中性轴张开**。

**这直接解释了你报告里被判为"误报"的那批警告：**

> - 34 个 LUT 提示中性轴颜色分离
> - 34 个镜头有非单位 RGB 白平衡增益

数量完全一致。**这不是 QC 误报，是 QC 抓到了真实缺陷。**（P0-2 的分层 QC 仍然要做，因为曝光导致的"超范围采样"警告确实是误报 —— 但中性轴那批是真的。）

### 修正方案（推荐第 3 种）

**方案 1（最小改动）**：把 WB 转成对数域 offset。
```python
# ACEScct 斜率约 1/0.0729 per stop，需按实际编码曲线标定
offset_i = np.log2(gain_i) * ACESCCT_STOPS_TO_CODE
cdl.setOffset([off_r, off_g, off_b])
```
快，但 ACEScct 在暗部有线性段，offset 在 toe 区仍不严格等价。

**方案 2（正确且简单）**：在进入 working space 之前的**线性域**插一个对角矩阵。
```python
group.appendTransform(ocio.MatrixTransform(
    matrix=[g_r,0,0,0, 0,g_g,0,0, 0,0,g_b,0, 0,0,0,1]))   # 在 reference (线性 AP0) 域
group.appendTransform(ocio.ColorSpaceTransform(src=reference_space, dst=working))
```
中性轴严格保持，一行改动量级。

**方案 3（专业正解）**：白平衡改为色适应变换（CAT）。

```
估计场景光源 → (CCT, Duv) → 白点 xy
→ Bradford / CAT02 矩阵 (源白点 → D65)
→ 在 AP0 线性域用 ocio.MatrixTransform 施加
```

好处远超"修 bug"：

- 中性轴严格不动，肤色不会被拉歪（当前逐通道 gamma 对肤色的伤害最大，因为肤色恰好在中间调）；
- `temperature` / `tint` 终于有真实物理含义（Kelvin / Duv），可以直接给调色师用；
- Hero Shot 匹配可以在 CCT/Duv 空间做，这是**镜头匹配的正确度量**（当前在 RGB gain 空间做匹配，距离度量没有感知意义）；
- 场景分组也能用 CCT/Duv 作为特征维度（见 F 节）。

`colour-science` 已在依赖里，`colour.temperature.CCT_to_uv`、`colour.adaptation.matrix_chromatic_adaptation_VonKries` 直接可用。

---

## B2 — `temperature` 是线性近似，没有物理基础 🟠 P1

```python
temperature = recipe.temperature / 50.0
effective_wb = [rgb_gains[0] * (1.0 + 0.08*temperature),
                rgb_gains[1] * (1.0 + 0.08*recipe.tint),
                rgb_gains[2] * (1.0 - 0.08*temperature)]
```

R 加 / B 减的线性近似，与 Planckian locus 无关，`tint` 只动 G 通道（真实的 tint 是垂直于黑体轨迹的 Duv 方向，会同时影响三通道）。

同时 `image_analyzer` 里 `estimated_temp_offset = gain_b - gain_r` 也是一个无量纲的编造量，却被 `scene_grouping` 当作"色温"维度参与分组，被 `project_qc` 当作"白平衡跳变"的判据。**一个错误的度量污染了三个下游模块。**

**修正**：并入 B1 的 CAT 方案。`temperature` 语义改为 ΔCCT（Kelvin），`tint` 改为 ΔDuv。分组和 QC 的距离度量随之改为 CCT/Duv 空间的欧氏距离（这才是感知均匀的）。

---

## B3 — 对比度 pivot 在 ACEScct 中的位置存疑 🟠 P1（需先验证）

```python
exposure_contrast.setStyle(ocio.EXPOSURE_CONTRAST_LOGARITHMIC)
exposure_contrast.setPivot(recipe.pivot)   # recipe.pivot 默认 0.18
```

数据此时在 ACEScct。18% 中灰在 ACEScct 中的编码值是 **≈0.4135**，不是 0.18。若 OCIO 的 LOGARITHMIC style 把 pivot 当作**已编码的码值**处理，那么支点落在了远低于中灰的位置 —— 提对比时中间调整体被压暗、暗部被过度拉开，这与"提对比后画面发闷"的主观感受吻合。

**这一条我无法从代码静态确认，必须实测。** 建议写一个 5 行的单元测试：

```python
def test_contrast_pivot_is_stationary():
    """对比度为任意值时，中灰 0.18 (linear) 应保持不变。"""
    for contrast in (0.9, 1.0, 1.15):
        recipe = GradeRecipe(contrast=contrast, exposure=0.0)
        out = apply_group_to_linear_rgb(np.array([[0.18, 0.18, 0.18]]), recipe)
        assert np.allclose(out, 0.18, atol=1e-3), f"pivot drift at contrast={contrast}"
```

不过则把 `pivot` 改为 ACEScct 编码域的 0.4135，或改用 `GradingPrimaryTransform`（其 pivot 语义在 log 模式下有明确定义）。

---

## B4 — 分析用 HSV 的 S 作为饱和度指标 🟡 P2

```python
hsv = cv2.cvtColor(img, cv2.COLOR_BGR2HSV).astype(np.float32) / 255.0
sats.append(hsv[:, :, 1].ravel())
```

HSV 的 `S = (max−min)/max`。分母是 max 通道 → **暗部像素的 S 极不稳定**，噪声就能让接近黑的像素报出 S≈0.8。后果：

- 夜景/暗调镜头的 `sat_mean` 被暗部噪声抬高 → `correction_engine` 判定"色彩丰富"而不做补偿；
- 或反过来 `sat_p95 > 0.90` 触发"高饱和溢出风险"误报；
- `grade_decision` 里 `saturation_risk = (sat.p95 - 0.82) * 1.8` 直接建立在这个不稳定量上。

**修正**：改用线性域的 CIELAB `C*ab` 或 ICtCp 的 chroma，按亮度加权统计（低于某亮度阈值的像素不计入）。`colour-science` 已可用。

---

# 第三部分：决策层 —— 让它更像调色师

## C1 — 曝光目标"全画面亮度中位数 → 0.40"对所有内容一视同仁 🔴 P1（主观提升最大）

```python
lum_med = report.luminance.median
if lum_med < 0.25:
    exposure_adj = min((0.40 - lum_med) * 2.0, 0.8)
```

**没有调色师是这么定曝光的。** 全画面中位数会被大面积天空、暗背景、夜景、雪地、字幕黑底主导 —— 这些恰恰是最不该被"拉回 0.40"的内容。

真实做法是**锚点驱动**：

| 场景 | 锚点 | 目标值（Rec.709 显示域） |
|---|---|---|
| 有人脸 | 肤色区域亮度（`face_analysis` 已经在算 `mean_skin_luminance`） | 55–70 IRE，即 0.55–0.70 |
| 无人脸、有明确主体 | 主体区域（中心加权） | 按 `content_class` 分档 |
| 风景/大景 | 高光滚降点 + 暗部落点双端对齐，不动中位数 | — |
| 夜景 / `content_class=black_or_near_black` | 不动（已有 `preserve` 逻辑） | — |

`face_analysis.py` 已经输出 `mean_skin_luminance` 和 `mean_skin_hue`，但 `correction_engine` **完全没用**。这是现成的、最有价值的信号被浪费掉了。

**建议重构 `generate_auto_correction_advice` 签名：**

```python
def generate_auto_correction_advice(
    report: ImageAnalysisReport,
    face: FaceAnalysis | None = None,          # 新增
    content_flags: dict | None = None,          # 新增
    spatial_weight: np.ndarray | None = None,   # 新增：中心加权 / letterbox 掩码
    ...
) -> AutoCorrectionAdvice:
    if face and face.face_count and face.face_area_ratio > 0.015:
        # 肤色锚点优先
        target = 0.62
        current = face.mean_skin_luminance
        exposure_adj = np.log2(target / max(current, 1e-3))    # 线性域下的真实 EV
        rationale = f"以肤色亮度 {current:.3f} 为锚点对齐到 {target}（{exposure_adj:+.2f} EV）"
    else:
        ...
```

注意 `np.log2(target/current)` 才是真实的 EV，前提是 A3 已经改到线性域。

**同时：肤色色相也应该成为白平衡的约束。** 调色师做 WB 的最终判据往往是"肤色落在 vectorscope 的 skin tone line 上"。有脸时，可以在灰世界解的基础上加一个正则项，把肤色色相拉向 IQ 线（约 123°）。这比纯灰世界稳健得多。

---

## C2 — 低置信度镜头直接放弃校正，是组内跳变的直接来源 🔴 P1

```python
if agreement >= 0.60:
    selected = np.median(candidates, axis=0)
else:
    gain_r, gain_g, gain_b = 1.0, 1.0, 1.0
    selected_method = "preserve_low_confidence"
```

安全，但代价是：**混光、大面积单色、特写这些最难估计的镜头完全不校正**，而同组其他镜头校正了 → 组内白平衡跳变。这精确对应报告里的"15 个白平衡跳变，其中 14 个发生在 SceneGroup 内部"。

**调色师的做法是相反的**：单镜头估不准时，不是放弃，而是**继承同场景的判断**。

**修正**：把 WB 决策从"每镜头独立"改为"组内层级"：

```
1. 组内所有镜头各自估计 WB + 置信度
2. 用置信度加权，求出 SceneGroup 的共识白点（robust weighted median in CCT/Duv space）
3. 高置信度镜头：自身解 × 0.7 + 组共识 × 0.3（向共识收缩）
4. 低置信度镜头：直接用组共识
5. 与组共识偏离超过阈值的高置信度镜头 → 标记为"可能是不同光源"，触发拆组
```

这一条同时解决三个问题：组内跳变、低置信镜头失校正、以及给 SceneGroup V2 提供拆组信号。

---

## C3 — 每镜头只取 3 帧，且是全画面无权重统计 🟠 P1

```python
return [round(usable_start + (usable_end - usable_start) * f, 6) for f in (0.2, 0.5, 0.8)]
```

两个问题：

**(a) 3 帧对运动镜头不够。** 摇镜、跟拍、变焦的镜头，首尾内容可能完全不同。建议按时长自适应：`n = clip(int(duration / 1.5), 3, 9)`，并对帧间统计取**稳健中位数**（防止一帧闪光/切光污染整个镜头的配方）。

**(b) 全画面统计被无关区域主导。** 应该加空间权重：

- 排除 letterbox / pillarbox 区（`classify_special_content` 已经检测出来了，但**只用于跳过决策，没有用于掩码统计** —— 又一个现成信号被浪费）；
- 中心加权（高斯，σ ≈ 0.35 × 画幅），因为主体通常在中心；
- 排除检测到的字幕/图形区（可用边缘密度 + 时间稳定性简单检测）。

**这个改动成本很低（一个 mask 参数传进 `analyze_image_frames`），但对准确性的提升相当直接。**

---

# 第四部分：渲染精度

## D1 — 全链路 8-bit + 33³ LUT + 无 dither → banding 🟠 P1

```python
filters = f"{lut_filter},{scale}:in_range=auto:out_range=tv,format=yuv420p"
```

`lut3d` 在 8-bit yuv420p 上做四面体插值，输出仍 8-bit。强 Look（尤其 `cinematic_soft_print` 的 `highlight_softness=0.65`）在天空、渐变、暗部必然 banding —— 报告里已经观察到 11 个 LUT 的 banding 警告。

**修正**：让 LUT 在浮点/高位深域运算。

```python
# 正式渲染
-vf "format=gbrpf32le,lut3d=file='...':interp=tetrahedral,format=yuv420p10le"
# 或 8-bit 交付时至少加 dither
-vf "format=yuv444p12le,lut3d=...,format=yuv420p"
-sws_flags "+accurate_rnd+full_chroma_int"
```

**LUT 尺寸**：技术校正层 33³ 足够（都是平滑的全局算子）；创意层特别是 CanonCGT 拟合结果建议 **65³** —— `lut_fitter` 拟合的是 11 项多项式，33³ 采样会在色域边缘丢失曲率。

---

## D2 — 技术层与创意层在 8-bit 域被合成成一张 LUT 🔴 P1

**位置**：`cube_tools.py::compose_cube_luts`

```python
first_rgb = first.reshape(-1, 3)          # 已 clip 到 [0,1] 的技术 LUT 输出
styled = _sample(second, first_rgb)       # 拿去查创意 LUT
composed = first_rgb + (styled - first_rgb) * amount
return write_cube(np.clip(composed, 0.0, 1.0), ...)
```

问题：

1. **中间 clip 不可逆**。技术校正若把高光推过 1.0（曝光 +0.8 EV 时必然发生），信息在合成的第一步就永久丢失，创意 LUT 拿到的是已经削平的高光。
2. **两次网格量化**。33³ → 采样 → 33³，误差叠加。
3. `_sample` 用 `RegularGridInterpolator` 默认线性插值，而渲染时 FFmpeg 用 tetrahedral —— **合成用的插值方式和渲染用的不一致**，这本身就是误差源。

**修正（推荐顺序）**：

1. **最佳**：不合成。FFmpeg 里串两个 `lut3d` filter，中间保持浮点：
   `format=gbrpf32le,lut3d=tech.cube,lut3d=creative.cube,format=yuv420p10le`
   代价是 CPU 时间翻倍（当前 LUT 已是 CPU 瓶颈），但正确。
2. **折中**：合成时技术 LUT 用扩展定义域（`.cube` 规范允许 `DOMAIN_MAX > 1.0`），中间不 clip，合成网格用 65³，插值改 tetrahedral 与渲染一致。

---

## D3 — 无母版输出档 🟡 P2

唯一输出是 8-bit H.264 CQ19。带强 Look 的片子交付即劣化，且无法回到 Resolve 精调。

建议三档：

| 档位 | 编码 | 用途 |
|---|---|---|
| Preview | H.264 720p CQ23 | 页面预览 |
| Delivery | H.264 / HEVC 8-bit CQ19 | 现有默认 |
| **Master** | ProRes 422 HQ / DNxHR HQX / x265 10-bit CRF16 | 归档、二次调色 |

`build_timeline_render_command` 加一个 `output_profile` 参数即可，改动很小。

---

# 第五部分：分层 QC 的具体口径（你确认要做的第 2 项）

核心原则：**按 transform 的角色分流，且技术层的 QC 应该看画面而不是看网格。**

| 层 | 应该检查 | 明确不检查 | 判据 |
|---|---|---|---|
| **Base Correction**（技术） | 应用后代表帧的：肤色 ΔE2000、中性物是否回到中性、黑白位、legal range、组内一致性 | ❌ LUT 网格的中性轴保持（技术 WB **必然**移动中性轴，B1 修好后是"正确地移动"）<br>❌ `[0,1]` 出界（曝光提升必然出界，`CDL_NO_CLAMP` 本来就是有意的） | 肤色 ΔE2000 < 3；黑位漂移 < 2 码值 |
| **Creative Transform** | 网格平滑度（二阶差分 p99）、单调性、色域外映射比例、**肤色区域的色相旋转量** | ❌ 中性保持（look 可以有意偏色）<br>❌ 应用后的绝对亮度（look 有意改变） | 二阶差分 p99 < 阈值；肤色色相旋转 < 8° |
| **Combined / 合成后画面** | banding 指标、clipping、镜头间连续性 | — | 组内 ΔL* < 5、ΔCCT < 300K |

**关键改动**：`project_qc.py:202` 现在是

```python
technical_color = "FAIL" if errors else "NEEDS_REVIEW" if clipping else "PASS_WITH_WARNINGS" if lut_warnings else "PASS"
```

`lut_warnings` 不分角色地拉低整体状态。改成按 `TransformAsset.transform_type`（字段已经有了：`technical / creative / combined / output`）分流到不同检查器，各自产出独立状态，再汇总。

**顺带**：`color_metrics.py` 用 ΔE76 + OpenCV 的 8-bit Lab（隐含 sRGB 假设）。肤色评估必须升级到 **ΔE2000**，且在真实色彩空间下算（`colour.difference.delta_E_CIE2000`）。ΔE76 在肤色所在的橙色区域误差很大，会漏报。

**预期效果**：48 个警告降到个位数，且每个都可操作。

---

# 第六部分：SceneGroup V2（你确认要做的第 3 项）

## 当前实现的三个结构性问题

```python
for shot in analysis.scenes[1:]:
    current = partitions[-1]
    if len(current) < max_group_shots and _shot_distance(current[-1], shot) <= 0.48:
        current.append(shot)
    else:
        partitions.append([shot])
```

1. **只比较相邻镜头**。对话戏的 A–B–A–B 正反打是最常见的场景结构，当前算法在每次切机位时都可能断组。
2. **`max_group_shots=12` 参与主逻辑**。这是报告里"多个组恰好 12 个镜头"的直接成因 —— 分组被容量而非内容决定。
3. **距离度量建立在错误的量上**。`temperature` 用的是 `gain_b - gain_r`（见 B2），`saturation` 用的是 HSV 的 S（见 B4）。**修好 B2/B4 之前，改分组算法收益有限。**

## V2 设计

**特征向量**（每镜头，全部在线性域）：
```
[ log2(肤色锚点亮度 或 中心加权亮度中位数),
  CCT (Kelvin, 对数化), Duv,
  CIELAB C*ab 的 p50 / p95,
  构图 embedding (DINOv2 或现有 offline backend),
  人脸占比, 人脸数,
  主色调直方图 (Lab ab 平面, 16 bin) ]
```
各维按全片标准差归一化。

**图构造**（关键改动）：不限于相邻。
```python
edge_weight(i, j) = exp(-feature_distance(i,j)² / 2σ²) * time_decay(|t_i - t_j|)
time_decay(Δt) = exp(-Δt / τ),  τ ≈ 20~30 秒   # 衰减很缓，允许跨镜头连边
```
这样 A–B–A–B 的正反打会因为 A₁–A₃ 的强边而聚在一起。

**聚类**：谱聚类或 Leiden 社区发现，不用阈值切分。簇数可由特征值间隙（eigengap）自动确定。

**约束与后处理**：
1. 连续时间块内的镜头给一个强先验（加一条固定权重的边），避免聚类打乱时序；
2. `max_group_shots` **降级为纯性能保护** —— 只在某组超过 N 时对该组做二次细分，不参与主聚类；
3. **后验校验（最重要）**：分组完成后，计算每组内的 ΔCCT 方差和 ΔL* 方差。超阈值时：
   - 若能二分且二分后方差显著下降 → 自动拆组
   - 否则 → 标记该组"需要 Shot Match"并触发 C2 的组内 WB 收缩

第 3 步是"组内 14 个 WB 跳变"的直接闭环 —— 分组算法自己检查自己的输出。

## Hero Shot 选择也要改

```python
return min(shot.duration, 8.0) - clipping * 20.0 - abs(index - center) * 0.1
```

当前是"时长长 + 裁切少 + 靠中间"。但 **Hero Shot 的用途是给全组做匹配基准，它必须是这一组的统计中心**，不是最长的那个。

建议加权重最高的一项：**到组内特征中位数的距离**。

```python
score = (- 2.0 * distance_to_group_median(shot)      # 最重要：统计代表性
         + 0.5 * min(duration, 8.0) / 8.0            # 次要：时长（长镜头统计更稳）
         - 1.0 * clipping_ratio                       # 惩罚：裁切
         + 0.3 * has_face                             # 加分：有脸的镜头更适合做肤色基准
        )
```

---

# 第七部分：执行顺序

## P0 —— 1~2 天，收益最大

| # | 项 | 位置 | 预期效果 |
|---|---|---|---|
| 1 | 代理和帧缓存补齐色彩元数据 + 无损化 | `frame_sampler.py` | 消除系统性色相/饱和度偏差 |
| 2 | **白平衡改线性域 CAT** | `ocio_manager.py::build_grade_group` | 中性轴不再张开，肤色不被拉歪 |
| 3 | 对比度 pivot 单元测试 + 修正 | `ocio_manager.py` + 新测试 | 中间调不再被误压 |

## P1 —— 3~5 天

| # | 项 | 位置 |
|---|---|---|
| 4 | 统计改线性域 | `image_analyzer.py` |
| 5 | 肤色锚点驱动曝光 + 肤色色相约束 WB | `correction_engine.py` |
| 6 | 组内 WB 层级收缩（低置信继承共识） | `job_worker.py::_apply_shot_match` |
| 7 | 代表帧空间加权 + 自适应帧数 | `scene_analysis.py` |
| 8 | 渲染链浮点中间 + 双 lut3d + 10-bit 母版 | `renderer.py` / `timeline_renderer.py` |

## P2 —— 1~2 周

| # | 项 |
|---|---|
| 9 | 分层 QC（第五部分） |
| 10 | SceneGroup V2 图聚类 + Hero Shot 重排（第六部分） |
| 11 | ΔE76 → ΔE2000，色彩空间正确化 |

---

# 第八部分：验证方法（没有这个，上面全是空谈）

**调色系统改不动的根本原因是没有 ground truth。** 建议在动手前先建：

### 1. 合成 Golden Set（半天，必做）

用 `scripts/generate_test_media.py` 扩展，程序化生成：

- 24 色 ColorChecker 图卡 + 已知的偏色/曝光偏移（真值已知）
- 灰阶梯（21 级）用于检验中性轴和 banding
- 已知 CCT 光源下的肤色样本（3 种肤色 × 5 个色温）
- 每个样本存 16-bit PNG，避免 8-bit 量化污染测量

**验收指标**：
```
中性轴保持       : 灰阶梯经技术校正后，各级 |a*|, |b*| < 1.0
白平衡准确度     : 已知 CCT 偏移的图卡，校正后白点 ΔCCT < 150K, ΔDuv < 0.002
肤色保护         : 肤色样本经 Look 后 ΔE2000 < 3.0，色相旋转 < 8°
往返精度         : 空配方（全默认）往返 PSNR > 50dB, ΔE2000 < 0.3
代理一致性       : 原片统计 vs 代理统计的 24 色块 ΔE2000 < 1.0
```

`tests/test_neutral_roundtrip.py` 和 `test_lut_precision.py` 已有雏形，扩展即可。

### 2. 真实素材回归（每次改动后）

固定 3 段素材（室内混光对话戏、外景大光比、夜景/舞台），每次改动后跑完整流程，对比：

- 组内 ΔCCT 方差、ΔL* 方差（应单调下降）
- LUT 警告数按类型分布（技术类应降到 0，创意类保持）
- 肤色 ΔE2000 分布

### 3. 盲测（阶段性）

改动前后的成片随机排序，人工二选一。这是唯一能验证"C1 肤色锚点曝光"这类主观改进的方法 —— 客观指标测不出"好看"。

---

# 附：三个"现成信号被浪费"的地方

改造时顺手接上，几乎零成本：

1. `face_analysis.mean_skin_luminance` / `mean_skin_hue` —— 已在计算，`correction_engine` 完全没用（见 C1）
2. `classify_special_content` 检测出的 letterbox 区域 —— 只用于跳过决策，没有用于掩码统计（见 C3）
3. `TransformAsset.transform_type` 字段（`technical/creative/combined/output`）—— 已定义，`project_qc` 没有据此分流（见第五部分）

---

# 最后一句

现有架构不需要推翻。`OCIOManager` → `bake_3d_lut` → `timeline_renderer` 这条主干是对的，`Baker` 的 `input_space → reference → target` 链路也是正确的。

要修的是**灌进这条主干的数据**（第一部分）和**主干上那几个算子的数学**（第二部分）。B1 一条改完，你现有 QC 报告里那 34 个"中性轴分离"警告应该会直接归零 —— 那会是最好的验证信号。
