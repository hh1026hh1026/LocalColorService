# V0.6.9 — 新视频测试前的状态

测试：**224 passed / 2 failed**（此前 213 passed / 6 failed）。剩下 2 个在你的机器上应该也会通过——它们需要 torch 和 CanonCGT 权重，沙箱里没有。

---

## 1. 修好了长期失败的测试，顺带发现渲染路径从没被测过

那 6 个"环境性失败"里有 4 个其实是同一个原因：`-fps_mode` 是 FFmpeg 5.0+ 的选项，沙箱是 4.4。

这不只是测试环境问题——**它意味着整条渲染主路径在测试里从来没有被真正执行过**。我这几轮改的浮点 LUT 链、输出档位、进度上报、取消机制，全都在渲染路径上，而覆盖它们的 4 个测试一直是红的，被当成"环境问题"跳过了。

加了版本探测（`supports_fps_mode`），老版本自动回退 `-vsync`。现在这 4 个测试真跑起来了：

- `test_video_acceptance_contract` —— 完整渲染 + 交付契约
- `test_chinese_and_spaces_path_handling` —— 中文路径
- `test_project_ccc_lut_qc_and_timeline_render` —— 时间线渲染 + CCC 导出
- `test_video_timeline_and_blend_strength` —— 时间线 + 强度混合

你的 ffmpeg 是 2025-01-15 的构建，本来就支持 `-fps_mode`，所以行为不变；但对方如果换台机器用老 ffmpeg，现在不会再抛 `Error splitting the argument list` 这种看不出原因的错。

---

## 2. 新增端到端冒烟测试

`tests/test_pipeline_smoke.py` —— 合成一段四场景素材（暖内景 / 冷外景 / 欠曝 / 过曝），跑完整链路：镜头检测 → 分析 → 配方 → 场景分组 → 白平衡协调 → 烘 LUT → 时间线渲染 → QC。

之前所有测试都是单阶段的。而实际出问题的两次（链式 LUT 打断人脸渲染、代理色彩元数据缺失）**都是阶段之间的接口问题**，单元测试看不见。

它立刻抓到了一个真 bug（见下）。

### 冒烟测试的实际输出

```
shot        曝光      余量   锚点            CCT    增益                  滚降
shot_0001  +0.000   0.800  frame_median   5050  [0.8, 1.0, 1.25]      0.00   暖内景
shot_0002  +0.000   0.800  frame_median   5162  [0.813, 1.0, 1.217]   0.00   冷外景
shot_0003  +0.800   0.800  frame_median   5050  [0.8, 1.0, 1.25]      0.60   欠曝→提亮+滚降
shot_0004  -0.800   0.141  frame_median   5735  [0.917, 1.0, 1.137]   0.00   过曝→压暗，余量仅0.141
```

四种情况被正确区分：欠曝的拿到 +0.8 EV 和高光滚降，过曝的余量只有 0.141（系统正确判断它没有提亮空间）。

---

## 3. 冒烟测试抓到的 bug：中性轴指标在近黑区读噪声

`shot_0004`（−0.8 EV 压暗）的 technical LUT 报 **drift = 0.085**，超过 0.040 阈值两倍多。

逐采样点查下来：

```
位置 0.219 之后的每个采样点   rg 色度 = (0.3176, 0.3313)   ← 完全一致，白平衡是对的
位置 0.156                 rg 色度 = (0.2326, 0.3353)   ← 输出 RGB 只有 0.044
```

**变换是正确的，是指标在读噪声。** 那两个采样点被 −0.8 EV 压到了 ACEScct 趾部的近黑区，那里编码不是幂律，微小的绝对差异会造成巨大的色度摆动。我原来的排除下限 `>0.001` 定得太低。

改成 `NEUTRAL_AXIS_OUTPUT_FLOOR = 0.05`。验证：

| | 修正后 drift |
|---|---|
| 合法技术配方（含 −0.8 EV + 强 WB + 对比度） | 0.000 – 0.032（全部 PASS） |
| **旧版对数域 slope 缺陷** | **0.068 – 0.157（3/3 仍被抓到）** |

**检测能力没有削弱。** 这条已经写成回归测试。

如果不修，你新视频里每个压暗的镜头都会误报。

---

## 4. 统一了两处不一致的限幅器

`group_white_balance` 上一轮已经改用 mired 限位，但 `correction_engine` 还在用会饱和的增益夹取 `[0.80, 1.25]`。同一个系统里两套限幅逻辑，而且其中一套有已知缺陷。

现在都用 `limit_white_point_shift`（±40 mired）。实测对比：

```
源片色偏      源CCT    校正后    位移
强暖         3937K   4674K   +40.1 mired
中暖         4649K   5712K   +40.0
弱暖         5873K   6506K   +16.6
中冷        10324K   7309K   -40.0
强冷        15311K   9491K   -40.1
```

单调、对称、不饱和。旧版会把 210/230/260 mired 三个不同目标全部塌缩到 198.0。

⚠️ **副作用**：强色偏镜头的增益范围变宽了（最远到 `[1.542, 1.0, 0.837]`），因为 ±40 mired 的位移本来就需要这样的增益。这不是失控——位移量仍然被严格限制，只是限制的对象从"增益数值"换成了"白点移动量"。相应的测试断言也改成检查位移量。

---

## 5. 新视频测试时建议看什么

### 先确认基础

```powershell
& C:\Users\Administrator\.conda\envs\localcolor\python.exe -m pytest -q
# 期望 226 passed（你的机器上 torch/CanonCGT 齐全）
```

### 跑完后重点看这几个

| 位置 | 期望 / 含义 |
|---|---|
| 日志里 `fell back because of a DEFECT` | **出现就是代码 bug**，不是环境问题，请贴给我 |
| 日志里 `Face-selective render unavailable` | 不应再出现。人脸保护这次是真的会跑 |
| `data/jobs/<id>/face_protection_mask.avi` | 应存在，且是 `480x270 gray`（1080p 源） |
| 界面进度条 | 应该滚动，不再固定 5% |
| `project_quality_report.json` → `large_exposure_corrections` | 新的人工校准标记，这批是最该人看的 |
| → `clipping_scenes` | 期望 0~2 个 |
| → `adjacent_temperature_jumps[].worsened_by` | 只列调色真正推开的接缝，带源片对照 |
| → `lut_warning_summary` | technical 那栏带警告数应接近 0 |
| `project_recipe.json` → `white_balance_diagnostics` | `inherited_from_neighbours` 有几个组是借的白点；仍为 `None` 的组值得人看 |
| 每个 shot 的 `exposure_diagnostics` | `anchor` 是 `skin_anchor` 还是 `frame_median`、`highlight_limited` 是否为真 |

### 生成复核清单

```powershell
& ...\python.exe scripts\build_review_sheet.py --render-job <job_id>
```

现在的类别是「大幅曝光 + 高光受限 + 肤色死白」，肤色色相降为次要（旧的 6° 阈值精确率只有 0.56，已放宽到 20°）。

### 如果渲染时间明显变长

人脸保护现在真的在跑了，会多一路 lut3d + maskedmerge。想临时关掉对比：`.env` 里设 `FACE_SELECTIVE_RENDER=0`。

---

## 6. 已知的、这轮没动的

| 项 | 状态 |
|---|---|
| 满屏肤色白平衡（79 mired） | 已知能力上限，靠 ±40 mired 限位和组共识兜底 |
| 2 个组连邻组也读不出光源 | 会保持不动并在诊断里说明 |
| B4：饱和度用 HSV 的 S | 暗部不稳定，尚未替换 |
| banding 真实检测 | `max_curvature` 已证明无区分力，需要测渲染后画面 |
| GPU lut3d（libplacebo） | 能同时解决人脸渲染的双 LUT 开销 |

以及一条方法论上的：`LARGE_EXPOSURE_CORRECTION_EV = 0.50` 是从**一位**评价人的 45 条判断拟合的，只能衡量误报不能衡量漏报。新视频的结果会是这个阈值的第一次外部检验——如果它在新素材上标出的镜头你觉得不对，那说明它过拟合了，告诉我。
