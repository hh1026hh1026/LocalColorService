# Local Color Service V0.3 + V0.5 验收报告

验收日期：2026-08-04

## 交付结论

V0.3 半自动 GradePlan 和 V0.5 CanonCGT 参考匹配已形成可从中文 WEB 工作台测试的完整闭环。V0.2 API 保持兼容；服务版本升级为 0.5.0。

## V0.3

- Shot 与可编辑 SceneGroup 分离。
- 自动场景分组建议和 Hero Shot。
- GradePlan v0.3：revision、状态、事件、审批人和审批时间。
- 镜头表可编辑场景组、曝光、色温、Look 强度和启用状态。
- revision 乐观锁防止覆盖过期编辑。
- 修改和候选选择自动撤销旧审批。
- 正式项目渲染默认要求批准；预览允许草稿。
- Provider、Candidate、TransformAsset 使用统一结构。

## V0.5

- 官方 CanonCGT 源码与 `SSL_updated_251111.pth` 已安装。
- RTX 3090 上完成真实推理冒烟测试。
- 参考图分辨率与裁切预检。
- 项目场景组自动抽取多镜头中间帧。
- CanonCGT 在技术校正后的代表帧上生成目标图。
- LUT Fitter 使用正则化、identity anchors、平滑和单调投影。
- 候选 A 为统计安全路线；B/C 为不同强度 CanonCGT 路线。
- 模型不可用或推理失败时显式统计回退。
- 候选选择可绑定 SceneGroup，随后进入 GradePlan revision/approval。
- 渲染时组合逐镜头技术 LUT 与场景级创意 LUT。
- CanonCGT 作用域默认全视频；单 SceneGroup 只改变该组，但统一生成整片预览。
- AdaInt 可为全部或指定 SceneGroup 生成创意 LUT，写回同一个 GradePlan 后整片预览。

## 2026-08-04 长时间线修复与流程复验

- 修复 179 镜头时间线触发 Windows `WinError 206`：FFmpeg 复杂滤镜改用 UTF-8 `timeline_filter.ffgraph`，不再把完整滤镜图放进命令行。
- 渲染前逐镜头检查组合 LUT；不稳定的创意 LUT自动回退到技术校正，仍不安全时回退中性 LUT，并在 `safety_fallbacks` 中留痕。
- 真实 354.6 秒、179 镜头项目整片预览完成，渲染 QC 与项目 QC 均通过；`scene_0147` 发生一次显式安全回退。
- AdaInt 场景组生成、整片预览完成；CanonCGT 默认全片生成、候选写回、整片预览、审批和正式渲染完成。
- 最新正式渲染验收产物：`data/jobs/139f64a476fc/project_graded_video.mp4`。

## 自动验证

```text
39 passed, 1 warning
```

唯一 warning 是已有 Starlette TestClient/httpx 弃用提示。

端到端 API 验收完成：

```text
镜头分析
→ 创建 reference_assisted GradePlan
→ CanonCGT A/B/C（无回退）
→ 选择 B 并绑定场景组
→ 批准 GradePlan
→ 正式时间线渲染
→ 渲染 QC 通过
→ 项目 QC 通过
```

最终组合 LUT 验收产物：`data/jobs/3a73494c8cdd/project_graded_video.mp4`。

中文工作台的 JavaScript 已通过 `node --check`。浏览器实测完成镜头分析、项目配方生成，并确认 AdaInt 与 CanonCGT 均默认选择全片作用域；页面控制台无错误。后端 API、真实模型、整片预览和最终渲染均已实际执行。

## 当前边界

- 仍限定 SDR Rec.709；Log/HDR 自动输入变换尚未实现。
- SceneGroup 第一版基于相邻镜头色彩统计，不声称语义理解，必须允许人工调整。
- CanonCGT 是图像模型；视频使用场景多帧拟合的全局 LUT，不逐帧运行模型。
- 无 SAM/局部遮罩，肤色和色域保护仍是全局安全约束。
- 65 阶 CanonCGT LUT 可用，但建议测试阶段先用 33 阶。
