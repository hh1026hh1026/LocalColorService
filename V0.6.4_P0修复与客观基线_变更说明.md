# V0.6.4 — P0-1 / P0-2 / P0-3 完成

测试：**172 passed**（此前 159），6 个失败仍是沙箱环境。

---

## P0-1 肤色 QC 度量

### 问题回顾

40 个肤色风险里 ΔE2000 中位 8.61，但 **ΔE2000 与 |EV| 相关系数 0.813** —— 判据被曝光污染了。

### 我先试错了一次

第一版打算用 `hypot(ΔC*, ΔH*)`（去掉亮度）。实测发现**不行**：

```
纯曝光 +0.8 EV：ΔL=17.95  ΔC=6.06  ΔH=0.02
```

CIELAB 的彩度 C\* 随亮度缩放，所以 ΔC 同样被曝光污染，只有 **ΔH 是干净的（0.02）**。

第二版试"先把亮度对齐再比色度"，结果 +0.8 EV 反而变成 13.81、色相转 50°。逐样本一查：**是最浅的肤色被推过 1.0**，亮度对齐反而把裁切失真放大了。

### 最终方案

逐肤色样本实测（六色面板，真实烘 LUT）：

| 样本 | 源 RGB | +0.8 EV 后 | 出界? | ΔH |
|---|---|---|---|---|
| 很浅 | 0.878/0.729/0.639 | 1.254/1.041/0.912 | 是 | **11.32** |
| 浅 | 0.800/0.616/0.514 | 1.142/0.880/0.734 | 是 | **7.80** |
| 中深 | 0.573/0.392/0.302 | 0.818/0.560/0.431 | - | **-0.04** |
| 深 | 0.435/0.286/0.220 | 0.621/0.408/0.314 | - | **-0.03** |

**没出界的样本 ΔH ≈ ±0.04；出界的 7.8–11.3。** 所以：

- **判据 = ΔH\*（阈值 3.0）+ 色相旋转（阈值 6°）** —— 与曝光无关
- **肤色出界（≥0.98）单列为 `skin_clipped`** —— 那不是"色相偏了"而是"颜色没了"，建议动作是降曝光而非降 Look
- ΔL / ΔC / ΔE2000 仍然记录，但不作判据

标定依据（六色面板实测）：纯曝光 ΔH<0.05、温度-16 达 -2.35、tint+0.25 达 -4.60、cold_thriller 4.9、day_for_night 16.5。色相干净地区分开了。

---

## P0-2 曝光高光保护

### 先测再做

`highlight_softness` 到底能买回多少余量？实测（同一亮度分布）：

| EV | soft=0.0 | soft=0.3 | soft=0.6 | soft=0.9 |
|---|---|---|---|---|
| +0.4 | 3.88% | 3.40% | 2.88% | **2.41%** |
| +0.8 | 8.57% | 8.29% | 7.99% | **7.63%** |

**滚降在高 EV 时几乎没用**（8.57→7.63）。所以不能靠它，必须直接压 EV。

百分位预测则很准：预测 3% 裁切对应 +0.461 EV，实测 +0.4 EV 得 3.88%。

### 实现

`exposure_headroom_ev(report)`：线性增益 `g=2^EV` 把亮度 `v` 映射到 `g·v`，超过 `1/g` 的部分裁切。用 `linear_p95`/`linear_p99` 对数插值反推 (1-budget) 百分位，得到 EV 上限。

- 预算：引入的高光裁切 ≤ 2%；源片本来就裁切更多的话，以源片水平为预算（不为相机原始素材的损伤背锅）
- 安全余量 0.10 EV（模型略乐观）
- 只夹取正 EV（压暗不会爆高光）
- 触顶时置 `highlight_softness=0.45` 并标记 `highlight_limited`

### 用真实数据验证

拿那 10 个爆掉的镜头重算：

| shot | 旧 EV | 余量 | 新 EV | 实测成片裁切 | 预测新裁切 |
|---|---:|---:|---:|---:|---:|
| shot_0080 | 0.800 | 0.386 | 0.386 | **21.71%** | **0.03%** |
| shot_0018 | 0.800 | 0.313 | 0.313 | 11.54% | 0.01% |
| shot_0017 | 0.770 | 0.332 | 0.332 | 10.97% | 0.02% |
| shot_0092 | 0.713 | 0.553 | 0.553 | 9.19% | 4.96% |
| … | | | | | |

**10 个全部改善。** 全片 136 镜头：15 个被高光余量限制，平均 |EV| 从 0.201 微升到 0.215（因为其他镜头的提亮没受影响）。

`highlight_limited=true` 的镜头就是**真正需要局部校正的那批** —— 全局曝光救不了"人暗天亮"，这一点系统现在会明说。

---

## P0-3 Golden Set + 冻结指标

### 产物

- `scripts/build_golden_set.py` —— 合成 24 色 ColorChecker + 21 级灰阶 + 六色肤色面板，各在 D65/3200K/4300K/9000K 四种光源下，16-bit PNG，共 148 KB
- `test_assets/golden/golden_set.json` —— 每个色块的精确真值
- `tests/test_golden_set.py` —— 13 项量化断言
- `docs/METRICS.md` —— **冻结的指标定义**

### Golden Set 立刻找到了两个真问题

| 项 | 结果 |
|---|---|
| D65 图卡光源估计 | **0.0 mired** ✅ |
| 4300K 图卡 | 40.0 mired ⚠️ |
| 9000K 图卡 | 30.8 mired ⚠️ |
| **3200K 钨丝灯** | **158.8 mired** ❌ |
| **满屏肤色 @ D65** | **99.8 mired** ❌ |

**缺口一：强钨丝灯下估计器直接放弃。** 三个候选分歧太大（agreement 0.482 < 0.60 门槛）→ 返回单位增益 → 读回来是 D65。

顺带发现并修了一个相关缺陷：`LINEAR_GAIN_MIN/MAX = [0.70, 1.45]` 只覆盖约 4200–9800K，3200K 需要蓝增益 2.66，必然饱和。已放宽到 `[0.35, 4.50]`（覆盖 2500–15000K）。

但**必须诚实说明：放宽夹取没有修好钨丝灯** —— 门槛在夹取之前就拒绝了。这是两个独立问题，我只修了其中一个。

同时明确了一条原则并写进文档：

> **测量范围宽（如实报告），应用范围窄（安全）。** 测量 `[0.35, 4.50]`，应用 `[0.80, 1.25]`。

以前用同一个常数兼任两职，所以"为了安全"的夹取悄悄变成了"测不准"。`group_white_balance` 的输出也补上了应用侧夹取（之前它绕过了安全包络）。

**缺口二：满屏肤色击败灰世界。** 人脸特写把主体当光源，D65 下估成 3944K，而且置信度 0.77 足以通过门槛，会把画面推冷。应用增益被 `[0.80,1.25]` 限制所以损害有界，SceneGroup 共识（C2）是设计上的缓解 —— 但单镜头层面确实会错。

两个缺口都以**当前实测值**钉进测试，只能变好不能变坏：

```python
def test_strong_tungsten_is_currently_abandoned_not_estimated():
    """KNOWN GAP - 估计器在 3200K 下放弃。..."""
    assert wb.selected_method == "preserve_low_confidence"

def test_a_frame_filled_with_skin_defeats_gray_world():
    assert error > 50.0, "如果这条开始失败，说明估计器变好了 —— 请收紧本测试"
    assert error < 140.0, "回归：肤色特写估计变差了"
```

### 冻结指标定义

`docs/METRICS.md` 记录了每个指标的定义、单位、标定依据、以及**已弃用的指标和弃用理由**。核心规则：

> 改算法不改指标。确需改指标时，新旧两套并行输出至少一个版本，并在变更说明里同时给出两套数字。

也记录了几个"想清楚了才不会踩"的点：ΔE2000 被曝光污染、ΔC 同样被污染、`max_curvature` 对 banding 无区分力（合法 LUT 曲率比缺陷 LUT 还高）、`estimated_temp_offset` 无量纲。

---

## 破坏性变更

1. **肤色风险的字段变了**：`skin_delta_e2000` 从判据降级为记录，新增 `skin_delta_hue` / `skin_hue_rotation_deg` / `skin_delta_lightness` / `skin_delta_chroma` / `category_detail`。QC 约束键从 `max_skin_delta_e2000` 改为 `max_skin_delta_hue`。
2. **曝光会比 V0.6.2 保守**，尤其亮背景镜头。这是有意的。
3. **白平衡测量范围放宽**，报告出的 CCT 可能出现更极端的值 —— 那是如实报告，应用仍受 `[0.80,1.25]` 限制。
4. 版本号 → **V0.6.4**。

---

## 重跑时该看什么

```powershell
& ...\python.exe scripts\build_golden_set.py      # 生成一次即可
& ...\python.exe -m pytest tests/test_golden_set.py -v
& ...\python.exe -m pytest -q
```

同一条 136 镜头素材重跑后对比：

| 位置 | 期望 |
|---|---|
| `clipping_scenes` | 从 10 个降到 0~2 个 |
| `skin_tone_risks` | 从 40 个降到个位数；剩下的 `category_detail` 应该基本是 `skin_hue_shift` 而非 `skin_clipped` |
| `highlight_limited` 的镜头 | 这批就是需要局部校正的清单 |
| `final_decision` | 若仍是 NEEDS_REVIEW，看是哪一项在驱动 —— 现在每一项都应该指向真问题 |

---

## 下一步

按当前证据，优先级是：

1. **修钨丝灯估计**（Golden Set 已量化：158.8 mired）。方向是让低置信度时不要放弃，而是输出带不确定度的估计交给 C2 共识去平滑；或者引入更稳健的估计方法（如 white-patch 与 gray-world 的加权融合）。
2. **人脸特写的白平衡**：有脸时应该降低灰世界的权重，或者用肤色先验约束（肤色应落在 vectorscope 的 skin tone line 附近）。这与 C1 的肤色锚点是同一类思路。
3. **多人协同评价** —— 现在客观基线立起来了，人评可以聚焦"好不好看"。建议把 `highlight_limited` 和 `skin_hue_shift` 的镜头单独抽出来给人看，那是系统自己承认拿不准的部分。
