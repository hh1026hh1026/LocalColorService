# V0.6 — A1 + B1 变更说明与测试指引

改动范围：色彩管理链路。没有触碰分组、QC、渲染编排、审批流程。

---

## 1. 改了哪些文件

| 文件 | 改动 |
|---|---|
| `color_core/white_balance.py` | **新增**。线性域 Bradford 色适应白平衡 |
| `color_core/ocio_manager.py` | 白平衡从 ACEScct 的 CDL slope 移到 reference 线性域的 MatrixTransform |
| `color_core/recipe.py` | `recipe_version` → `0.6.0`；新增 `white_balance` 溯源字段 |
| `color_core/frame_sampler.py` | 代理显式写入色彩标记；代理质量 CQ28→CQ20；帧缓存 JPEG→PNG；缓存键加 `-v2` |
| `color_core/media_probe.py` | 新增 `color_range` 探测 |
| `color_core/interchange.py` | ASC-CDL 导出的 slope 改用真实 CAT 矩阵的对角线 |
| `tests/test_white_balance_cat.py` | **新增** 12 项 |
| `tests/test_analysis_proxy_color_tags.py` | **新增** 6 项 |

测试：**64 passed**。6 个失败全部是沙箱环境所致（沙箱 ffmpeg 4.4 不支持 `-fps_mode`，CanonCGT 测试会调用 `C:\Users\...\localcolor\python.exe`），与本次改动无关，在你的 Windows 环境上不会出现。

---

## 2. B1 — 白平衡（核心改动）

### 改之前

```python
group.appendTransform(ColorSpaceTransform(reference → ACEScct))   # 进对数域
...
cdl.setSlope([rgb_gains × temperature 修正])                       # 在对数域逐通道乘
```

`rgb_gains` 的语义是**线性域的通道增益**，但被施加在 ACEScct 对数编码上。对数域的逐通道乘法等价于线性域的**逐通道 gamma**，不是白平衡。

### 改之后

```python
# reference space（ACES2065-1，线性）
group.appendTransform(MatrixTransform(bradford_cat_matrix))       # ← 新
group.appendTransform(ColorSpaceTransform(reference → ACEScct))
...
cdl.setSlope([recipe.gain])                                       # 只剩创意 CDL slope
```

分两层合成：

- **技术层**：`rgb_gains` → 反推场景光源白点 → Bradford CAT 适应到 D65
- **创意层**：`temperature` → mired 偏移，`tint` → Duv 偏移，同样走 CAT

### 实测验证

用 19 级中性灰阶跑完整 GroupTransform，在线性 AP0 域测色度：

| `rgb_gains` | 旧实现 x,y 漂移 | 新实现 x,y 漂移 |
|---|---|---|
| `[0.93, 1.0, 1.12]` | **0.106 / 0.076** | 0.0000073 / 0.0000108 |
| `[1.08, 1.0, 0.90]` | **0.172 / 0.107** | 0.0000084 / 0.0000088 |
| `[0.88, 1.0, 1.12]` | **0.171 / 0.085** | 0.0000062 / 0.0000109 |

参考尺度：D65 到 3200K 钨丝灯的色度距离约 Δx = 0.13。**旧实现在一个 12% 的白平衡修正上，就让暗部和亮部拉开了超过一整个 D65→钨丝灯的距离。** 新实现的残差是 float32 数值噪声。

其他已验证性质：

- 默认配方（gains=[1,1,1]、temperature=0、tint=0）→ **严格恒等矩阵**，逐像素误差 < 1e-4
- `strength=0` → 恒等；`strength` 线性插值
- CAT 把估计出的场景光源精确映射到 D65（色度误差 < 1e-4）
- 白平衡**不改变亮度**（Y 保持在 2% 以内）
- temperature +8 变暖 / −8 变冷方向正确；tint +0.25 出品红、−0.25 出绿

---

## 3. A1 — 分析代理色彩元数据

### 我需要更正上一份报告里的一个说法

上一份报告我把 BT.601/709 矩阵误判写成了"正在发生"。实测下来更准确的说法是：**这是一个条件性风险，不是必然发生的错误。**

实测（1920×1080 → 360p，libx264）：

- 当**源片带色彩标记**时，ffmpeg/libx264 会自动把标记传递给代理 → 旧代码在这条路径上其实是安全的
- 当**源片没有标记**时，代理也没有标记，此时同一份 YUV：

| 解码假设 | 灰世界增益 |
|---|---|
| 按 bt709 | `[1.0261, 1.0, 1.0187]` |
| 按 bt601 | `[0.9911, 1.0, 1.1111]` |
| ffmpeg 默认 | `[1.0473, 1.0, 1.0387]` |

矩阵选错导致的白平衡增益偏差最大 **0.092** —— 而系统允许的整个修正范围才 [0.88, 1.12]。同一帧的 ΔE76 均值 12.6、p95 31。

也就是说：**风险是真的，量级也确实很大，但只在源片未标记时触发。** 而你的 `test_assets/neutral_sample.mp4` 探测出来就是 `color_primaries: unknown, color_transfer: unknown` —— 说明未标记的素材在你的流程里确实存在。

另外注意 ffmpeg 默认解码给出的 `[1.0473, 1.0, 1.0387]` 和正确的 bt709 结果也不一致，仍有约 0.02 的增益偏差。

### 结论

A1 的价值是**消除对编码器标记传递行为的依赖**，成本几乎为零。它不是"修复一个必然存在的错误"，而是"堵掉一个在部分素材上量级很大的不确定性"。

我没能验证的一条：你实际用的是 `-hwaccel cuda + scale_cuda + h264_nvenc` 硬件路径，沙箱没有 GPU。硬件帧路径丢失元数据的概率比 libx264 高，**建议你跑一次后用 ffprobe 确认代理标记**（命令见第 6 节）。

### 顺带改的

- 代理质量 CQ 28 → 20（代理很小，28 在压掉白平衡最依赖的色度细节）
- 代表帧缓存 JPEG q92（4:2:0）→ PNG 无损。这批帧同时是 CanonCGT LUT 拟合的输入，色度子采样会被多项式的 `r*g`/`r*b`/`g*b` 交叉项学进去
- 缓存键加 `-v2`，**旧缓存全部作废**，第一次分析会重新生成

---

## 4. 破坏性变更（测试前请先看这里）

### 4.1 已批准的 GradePlan 需要重新批准 ⚠️

`recipe_version` 0.2.0 → 0.6.0，`recipe_hash` 随之改变。旧的冻结资产本身仍能通过 SHA 校验，但**用旧配方重渲染出来的画面和当初批准时不一样了**（色彩数学变了）。建议把历史批准视为过期，重新走一遍预览 → 批准。

### 4.2 Look Package 的 temperature 语义变了 ⚠️ 需要目视复核

旧：`temperature/50 × 0.08` 的线性 R/B 近似。
新：`temperature × 2` mired 的白点偏移（mired 是 CTO/CTB 滤色片的单位，1/8 CTO ≈ 20 mired）。

受影响的 Look：

| Look | temperature | 新的白点偏移 |
|---|---:|---|
| `clean_commercial` | 1.5 | +3 mired（几乎不可见） |
| `restrained_teal_amber` | 3.0 | +6 mired |
| `cinematic_soft_print` | 4.0 | +8 mired |
| `warm_memory` | 8.0 | +16 mired ≈ 1/8 CTO |
| `cold_thriller` | −10.0 | −20 mired ≈ 1/8 CTB |
| `day_for_night` | −16.0 | −32 mired ≈ 1/4 CTB |

这些数值我是按"专业上说得通"选的，不是按"和旧版观感一致"选的。**这几个 Look 的暖冷程度会和以前不同，需要你目视确认是否满意。** 不满意的话直接调 Look Package 里的 `temperature` 数值即可，现在它有明确物理含义了。

### 4.3 白平衡修正强度变大了 ⚠️ 这是测试时最该盯的

`image_analyzer` 的灰世界目前仍在 gamma 编码域估计（那是 A3，本次没做）。编码域的增益要变成物理增益必须做幂次转换，所以新代码对 `rgb_gains` 取了 2.2 次幂：

```
gains 1.12（编码域）→ 1.283（线性域）
gains 0.88（编码域）→ 0.756（线性域）
```

理论上这是对的 —— gamma 域的灰世界本来就系统性低估偏色。但这意味着**偏色修正的力度比以前明显更强**，如果某个镜头的灰世界估计本身就是错的（大面积单色、混光），过度修正会比以前更显眼。

**如果测试中发现偏色被修过头了**，在 `.env` 里加：

```ini
# 1.0 = 完全关闭线性化，回到与旧版接近的修正力度
# 2.2 = 默认，色彩学上正确
WB_GAIN_LINEAR_EXPONENT=1.8
```

这个旋钮不影响 B1 的核心修复（中性轴仍然严格保持），只影响修正的**幅度**。等 A3（统计改线性域）做完后，这个值应该设回 1.0，因为那时增益本身就是线性域的了。

### 4.4 ASC-CDL 导出

`.cc/.ccc` 的 slope 现在是真实 CAT 矩阵的对角线，不再是原始 `rgb_gains`。ASC-CDL 是逐通道算子，无法表达 3×3 的 CAT（我们测到的 R←G 交叉项有 −0.109），所以导出只是最佳对角近似。**需要精确交换时用 CLF 导出**，它烘的是真实 GroupTransform。

---

## 5. 测试时预期看到什么

### 应该改善

1. **QC 报告里的"中性轴颜色分离"警告应该归零**（原来 34 个，和 34 个非单位 `rgb_gains` 镜头数量完全吻合）。这是最直接的验证信号。
2. 有偏色的镜头，**暗部和亮部的色偏方向一致了**。旧版最典型的症状是"提亮阴影后阴影发蓝、高光发黄"这类矛盾偏色。
3. 肤色更稳。肤色在中间调，正是旧版逐通道 gamma 伤害最大的区间。
4. 未标记源片的白平衡估计不再受解码矩阵影响。

### 不会改善（本次没做）

- 曝光仍是"全画面亮度中位数 → 0.40"（C1）
- 统计仍在 gamma 域（A3）
- 低置信度镜头仍然直接放弃白平衡（C2）→ **组内白平衡跳变不会明显减少**
- LUT QC 仍未按 transform 角色分流（第五部分）→ "超范围采样"那批误报还在
- SceneGroup 仍是相邻贪心 + 12 上限

所以 QC 报告里预期是：**中性轴那类警告消失，其他类别基本不变。** 如果连续性跳变数也下降了那是意外之喜。

---

## 6. 建议的验证步骤

```powershell
# 1) 单元测试
& C:\Users\Administrator\.conda\envs\localcolor\python.exe -m pytest tests/test_white_balance_cat.py tests/test_analysis_proxy_color_tags.py -v

# 2) 全量回归
& C:\Users\Administrator\.conda\envs\localcolor\python.exe -m pytest -q

# 3) 确认代理确实带上了色彩标记（硬件路径，我没能在沙箱验证）
#    先跑一次分析，然后：
ffprobe -v error -select_streams v `
  -show_entries stream=color_space,color_primaries,color_transfer,color_range `
  -of default=nw=1 data\cache\analysis\<新的缓存目录>\proxy_360p.mp4
#    四项都应该有值，不能是 unknown

# 4) 拿之前那条 4分18秒 / 136 镜头的素材重跑
#    对比新旧 project_quality_report.json 里 lut_warnings 的分类计数
```

第 4 步是关键：同一条素材、同样的流程，**只看 LUT 警告里"中性轴"那一类的数量变化**。

另外 `project_recipe.json` 里每个 `base_correction` 现在会多出一个 `white_balance` 块，形如：

```json
{
  "method": "cat_bradford_linear",
  "gain_linearization_exponent": 2.2,
  "linear_gains": [0.852437, 1.0, 1.283157],
  "source_white_xy": [0.340323, 0.353791],
  "source_cct_kelvin": 5190.8,
  "source_duv": 0.003051,
  "target_white_xy": [0.3127, 0.329]
}
```

`source_cct_kelvin` 现在是可读的物理量 —— 你可以直接判断"这个镜头系统认为是 5190K"合不合理，这在以前是做不到的。它也顺便为后面的 SceneGroup V2 提供了正确的分组维度。

---

## 7. 下一步

按上一份报告的优先级，B1 修完后收益最大的是 **C2（低置信度镜头继承组内共识白点）** —— 它直接对着报告里"15 个白平衡跳变中 14 个在组内"这个问题，而且现在有了 `source_cct_kelvin` 作为正确的距离度量，实现起来比之前干净得多。

要不要先做 C2，还是按原计划推进 QC 分层和 SceneGroup V2，你测完再定。
